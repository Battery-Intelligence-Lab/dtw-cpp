# Diff ledger — ideal (spec Part II) vs as-is (2026-09-07 maps)

One row per as-is finding that the ideal design touches. Verdict ∈ KEEP (the
recorded rationale wins), CHANGE (what, why, the oracle that proves it, wave),
DEFER (owner / decision needed), DECIDE (Volkan). Source = report section.
Nothing here is authorised until the spec is approved.

Waves: W0 baseline/tooling · W1 base · W2 core · W3 algorithms+mip · W4 session
· W5 io · W6 cli/api · W7 tests · W8 docs.

## Core (from `.claude/reports/2026-09-07-asis-core.md`)

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| C-01 | `resolve_dtw_fn(const Problem&)` reads 5 members, stores a lifetime-critical reference, and is the `core/ → Problem.hpp` edge (§4a, §6.1 D1) | L1 never names `Problem`; bind from a value | CHANGE: `DistanceConfig{band, variant_params, missing_strategy, ndim}` by value + `const WdtwWeightCache&`; closure semantics become uniform (config captured at bind), which also removes the two staleness behaviours of D8. Oracle: conformance digit-identical + `unit_test_dtw_dispatch` | W2 |
| C-02 | Three resolvers (`resolve_dtw_fn`, `distance::dtw(params)`, `core::dtw_runtime`) encode the variant×missing product and already disagree (§6.1 D2) | one dispatch table | CHANGE: one `visit_variant(config, f)` template used by all three entry points; the one-shot paths call the kernel through it (no `std::function` construction per call). Oracle: existing pairwise tests + a new equivalence test fill-vs-facade on all variant×missing pairs | W2 |
| C-03 | Orientation/guard preamble copied 12+ times (§5 D1); band-feasibility preamble ×5 (D2) | helpers earn their place by deleting ≥2 copies | CHANGE: `core::orient(x, y)` 4-field aggregate (shape of `arow_orient`), callable stays a template parameter (DNB-1/DNB-2). Mechanical; digit-identical | W2 |
| C-04 | Pruned fill body ×2, drifted feature sets and different parallel shapes (§5 D3); header takes `Problem&` (§4a) | one implementation, narrow inputs | CHANGE: single pruned fill over `SeriesSource` (n, ndim, `series(i)`) + matrix sink + `{band, variant, LB strategy}`; delete the standalone copy. Depends on C-05 | W2 |
| C-05 | `Auto` selects `Pruned`, which LESSONS.md:160 proves does strictly more work than BruteForce on an exact matrix (§6.1 D5, Q1) | fastest default | DECIDE: recommend `Auto` = BruteForce for exact matrices; `Pruned` stays an explicit strategy for NN/TADPole-style uses. Band: cell-count counter + digit-identical matrix | W2 |
| C-06 | Dense `set()` lock-free vs mmap `set()` two atomic RMWs on row-shared words; `std::visit` twice per pair (§6.1 D3, DNB-11, Q4) | no per-pair branch; contention measured, not assumed | CHANGE the visit: hoist `visit_distmat` out of the pair loop (once per block). DEFER the digest batching until a benchmark of the two `set()`s under contention exists (none today) | W2 / perf |
| C-07 | Brute-force fill decomposes by row → triangular load; pruned path already uses pair-index blocks (§6.1 D4) | balanced lock-free decomposition, exposed as `parallel::for_each_pair` | CHANGE: brute-force fill uses the pair-block decomposition through the new helper. Digit-identical (independent writes); measure per-thread pair counts | W2 |
| C-08 | `-fno-finite-math-only` is `PRIVATE`; NaN contract lives in headers; a `-ffast-math` consumer gets silently wrong AROW/ZeroCost (§6.1 D10) | no silent wrong answers | CHANGE: make the flag `PUBLIC` for GNU/Clang front-ends; add a consumer-build test with `-ffast-math` that must still pass AROW tests | W0 |
| C-09 | Zero production callers (§7, corrected after a whole-repo grep): `core::dtw_distance` ×2 (test-only: `unit_test_invalid_distance_enums.cpp:341-351`, `unit_test_dtw_api.cpp`), `TimeSeries[View]` (test-only), `lb_*_valid` trait matrix (one entry documented wrong; also pinned as a contract string by `scripts/check_docs_contract.py:1039`), four MV/squared LB entry points (~180 lines; tests + `docs/content/method/multivariate.md`), dead convenience overloads, `MissingL1Dist`/`MissingSquaredL2Dist` (strictly dead — only their definitions), `MissingMV*Dist` (test-only), `sync()` ×2 (27 test/benchmark hits), `Float32+Mmap` declared-unimplemented. **Not a candidate:** `derivative_transform_mv` (returning overload) — seven test call sites and documented as public API at `docs/content/method/dtw-variants.md:41` | YAGNI, with the docs and the contract checker in the same commit | CHANGE: delete with the grep proof in the commit, updating the affected tests, `check_docs_contract.py` and docs pages together; MV lower bounds DECIDE with C-05 (wire or delete); full-matrix `dtwMissing`/`dtwAROW`/`softmin_gamma` are independent oracles → move to `tests/support` as oracle implementations, not deleted; `derivative_transform_mv` KEEP | W2 |
| C-10 | Six stale comments incl. a non-existent `z_normalize_simd.cpp` and "-ffast-math (this build)" (§7) | comments state invariants that hold | CHANGE: fix in the same commits as the code they sit in | W2 |
| C-11 | `error.hpp`, `missing_utils.hpp`, `settings.hpp` are stdlib-only foundation yet sit above `core/`; 9 upward includes (§4b, Q6) | L0 `base` | DECIDE: move to `dtwc/base/` in one mechanical wave with a script (churn across ~60 includes; bindings mostly include the umbrella) vs. keep paths and enforce layering by a file→layer manifest. Recommend the move: the include graph becomes acyclic by construction | W1 |
| C-12 | `dtwc::solver` tolerances/triplets live in `types/`, pulled into every TU via `utility.hpp` (§6.1 D6) | mip owns its types | CHANGE: move to `mip/`; add the missing `<cmath>` | W1 |
| C-13 | A user cannot inject a custom pairwise distance into a `Problem` (§9) although `dtw_fn_` is already a `std::function` | functional injection, zero hot-path cost | CHANGE (new, small): `Problem::set_distance_function(fn)` + invalidation of the WDTW cache + a `custom` tag in the mmap cache identity (or refuse mmap caching for custom functions). C++ compile-time path (Cost/Cell policies) documented as the extension API with an example | W4 |
| C-14 | `DTWVariant` is a closed enum guarded in five places; a new variant is a five-file change (§9) | one table | CHANGE: folds into C-02's dispatch table + one validation table | W2 |
| C-15 | `thread_local` scratch re-entrancy invariant is true by construction and written nowhere (§8, Q9) | invariants documented | KEEP the buffers; document the precondition on Cost/Cell policies at the kernel contract | W2 |
| C-16 | EAP prune slack factor 16 is regression-tested, not derived (DNB-6, Q8) | derivations own constants | KEEP; owner PLAN R2-D4 | — |
| C-17 | `.dtws` store lacks the crash-consistency machinery its sibling has (Q10) | either parity or explicitly ephemeral | DEFER: feature decision (outside quality scope); record in PLAN | — |
| C-18 | Banded kernel's three O(n) setup passes and EAP's two `assign`s (prior H1, Q5) | measure, don't reason | DEFER to a perf slice with `BM_dtwBanded` before/after | perf |
| C-19 | `MetricType::L2` unreachable from any token; it is the only metric whose MV behaviour differs (§7, Q7) | no unreachable public option | DECIDE: wire the token (`l2`/`euclidean`) with a test, or delete `MVL2Dist`+`SpanMVNanAwareL2Cost`. Recommend wire: Euclidean local cost is a legitimate MV choice | W2 |
| C-20 | Mmap v1/v2 migration strings for files no writer ever produced (§7) | YAGNI | CHANGE: delete (never released) | W2 |
| C-21 | `distance.hpp` facade includes every variant header; `matrix_io.hpp` drags llfio into dense CSV IO; `settings.hpp` pulls `<iostream>`/`<random>` everywhere (§4c) | thin includes | CHANGE: facade includes only the dispatch header after C-02; split dense vs mmap matrix IO headers; `settings.hpp` audit in W1 | W1/W2 |
| C-22 | Public mutable fields `variant_params`/`band`/`missing_strategy` with two staleness semantics (§6.1 D8) | one semantics | CHANGE: after C-01 the closure is a pure function of the captured config; `ensure_dtw_function_configuration_current` (exists) must detect field writes — verify, else privatise the fields (bindings use the setters; frozen surface unaffected) | W4 |
| C-23 | `core/dtw.cpp` ↔ `distance.hpp` cycle-shaped include (§4a) | acyclic | CHANGE: dissolves with C-02 | W2 |
| C-24 | `dtwc/CMakeLists.txt` PUBLIC header inventory is stale (§6.1 D9) | inventories are generated or absent | CHANGE: replace the list with a `file(GLOB … CONFIGURE_DEPENDS)` or delete it (include dir carries the contract) | W0 |
| C-25 | Standalone pruned reduction false-shares `PruningStats` rows (§6.1 D7) | per-thread accumulation | CHANGE: dies with C-04 | W2 |
| C-26 | `soft_dtw_gradient` keeps its own forward recurrence because the kernel does not retain the matrix (§5) | one recurrence | DEFER: a `RetainingCell`/matrix-capturing kernel variant is a design addition; keep the duplicate, mark it as the one known semantic duplicate | W2 note |

## Orchestration (from `.claude/reports/2026-09-07-asis-orchestration.md`)

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| O-01 | `dist_by_ind` preflights TWICE (`Problem.cpp:682` then `validate_mmap_cache_identity` at `:598`); the comment "Exactly ONE preflight per call" is false (§8 D1) | validation is a function of configuration; cache it | CHANGE: fast path = fixed-size snapshot compare only (already O(1), P5); the preflight runs only when the snapshot differs (slow path). Digit-identical; band = branch count per lookup measured with a counter test | W4 |
| O-02 | Every lookup revalidates semantics to detect raw public-field writes (§11 Q3); closures read live config through `*this` (P1) | closure = pure function of bind-time config; gateways validate | CHANGE (pairs with C-01/C-22): capture `DistanceConfig` by value at bind; gateways (`fill`, `cluster`, `scores::*`, oracle open) compare the snapshot and rebind on drift; `dist_by_ind` keeps only the O(1) snapshot compare. Semantics: effective config = config at last gateway — which is what `ensure_dtw_function_configuration_current` already enforces | W4 |
| O-03 | God object: ~174 members in 8 clusters; algorithms write `prob.clusters_ind`/`centroids_ind` directly (§8 D2); no private member is frozen (§10) | `Problem` = façade over owned components; algorithms return values | CHANGE in two steps: (1) algorithms return `ClusteringResult`, `Problem` adopts it (kills the writeback invariant Tier-1 `Result` silently relies on); (2) extract C2+C3 into an owned `DistanceCache{binding, matrix, identity}` behind unchanged setters. Oracle: conformance + every algorithm test | W3 → W4 |
| O-04 | `distanceInClusters()` issues ≈N²/2k already-computed lookups per Lloyd iteration (§8 D4) | no wasted work | CHANGE: delete call and method (single caller). Digit-identical | W4 |
| O-05 | `set_data` does not resize label buffers, `set_view_data` does; comment wrong (§8 D3) | one path | CHANGE: `resize()` in both; fix comment | W4 |
| O-06 | `set_max_iter(0)` / `set_n_repetitions` unvalidated → Lloyd reports a "converged" cost with zero iterations (§8 D7) | validate at API boundary | CHANGE: reject `n < 1` in both setters (frozen names, additive strictness; Tier-1 already validates) | W4 |
| O-07 | `writeMedoids` bypasses the checked open/close helpers (§8 D6) | one writer path | CHANGE: route through `open_output`/`close_output`; dissolves into O-09 | W5 |
| O-08 | DCL in `dist_by_ind`: unsynchronised `needs_init` read races the resize inside `critical(distByInd_init)`; `rebind_dtw_fn()` (writes 7 mutables) runs inside that critical (§6, OPEN since 09-02) | no lazy mutation on the hot path, or a correct DCL | CHANGE: `std::atomic<bool> matrix_ready_` (acquire on the fast path, release under the named critical); binding moves to the gateway so the critical only resizes. Deferred allocation (P10) preserved | W4 |
| O-09 | Three output-artefact schemas; Tier-1 and CLI agree on names/headers, `Problem_IO` writes a fourth, different set (§7 U1, Q5) | one writer per artefact | DECIDE: one `io::artefacts` module used by api, CLI and `Problem::write_*`; `Problem_IO`'s 1.x filenames (`_Nc_<k>.csv`, `medoids_rep_<r>.csv`, …) either dropped (contract addendum) or kept as a legacy writer. Recommend drop: `Result::save`'s four-file contract is the documented one | W5 |
| O-10 | Silhouette-skip warning duplicated byte-identically (U2); `Env::threads()` = `effective_max_threads()` (U5); `Data` constructors repeat checks (U7); mmap CSV emitter open-coded (U3); matrix CSV loop ×2 (U4) | DRY | CHANGE: mechanical dedup, each in the wave of its file | W4/W5 |
| O-11 | String-typed dispatch at Tier-1 (`method`/`device` strings validated then re-parsed 70 lines later); `MIPSettings::benders` is a string compared to literals in the library (§8 D5) | strings become enums at the surface | CHANGE (internal; signatures frozen): parse once at the boundary into `Method`/`Device`; `benders` → enum inside the library with the string accepted only at the CLI/binding edge (check whether Python sets the string field) | W4/W6 |
| O-12 | Tier-1 in-memory `cluster()` copies the whole dataset (`api.cpp:166`; MATLAB pays it per call) (§8 D14) | never copy | CHANGE: consuming overload `cluster(Dataset&&, …)` (additive); MATLAB binding may adopt later | W6 |
| O-13 | `Result::distance_matrix()` re-reads N² through `dist_by_ind` after the fill (§8 D15); `is_distance_matrix_filled()` is O(N²) per Lloyd iteration (§8 D16) | O(1) state | CHANGE: read the packed matrix directly; maintain a `filled` counter/flag instead of scanning for NaN | W4 |
| O-14 | `Data` has four modes encoded in three independent booleans (4 valid of 8 states) (§11 Q4); accessors must keep the predictable branch (P2) | states representable exactly | CHANGE: one `Storage` enum {HeapF64, HeapF32, View, MetadataOnly}; accessors `switch` on it (same branch, exact state space). No `std::variant` (wider refactor, no gain on the hot path) | W4 |
| O-15 | `DataLoader::load()` reads the global `Env`; `load_local()` exists to escape it (§8 D12); footprint scan + RAM syscall even when policy is Heap (D13); non-const getters force `Problem(name, DataLoader&)` (D8) | explicit inputs, const-correct | CHANGE: device passed in (the env-reading overload calls the explicit one); lazy footprint; const getters | W5 |
| O-16 | Three `friend`s: a test-only accessor, `load_checkpoint` reaching `distMat`, Benders needing the unpublished Lloyd run (§8 D9) | no test hooks in production; narrow public seams | CHANGE: `adopt_distance_matrix(DenseDistanceMatrix&&)` (additive) replaces the checkpoint friend; Benders calls the algorithm function (O-03 step 1) instead of the private impl; the test friend is replaced by observable-behaviour tests | W4 |
| O-17 | `s_bulk_read_invocations` test instrumentation compiled into the production loader (§9.2) | no test state in production | CHANGE: optional `LoadStats*` out-parameter (the `PruningStats` pattern) — keeps the perf fence ("one bulk read per load"), deletes the static | W5 |
| O-18 | `test_api.hpp` is production surface (bindings' capability probes) named as test scaffolding and the only header pulling the whole umbrella (§9.4) | names say what things are; thin includes | CHANGE: rename to `capabilities.hpp`, include only its two dependencies; `dtwc::test::` namespace kept (cross-language name); two binding include lines updated | W6 |
| O-19 | Unreachable code: `LowerBoundStrategy::None` in the pruned path, `checkpoint.cpp` always-false bound and two dead `catch`es, two defensive throws in `api.cpp` (§9.2); stale comments incl. `main.cpp` repo-relative path (non-negotiable #1) (§9.3) | YAGNI; comments true | CHANGE: delete/fix; `main.cpp` takes the path as an argument | W4–W6 |
| O-20 | `checkpoint` is a 12th public `Problem` field not in the contract's list of eleven (§8 D11) | contract truthful | CHANGE (docs): addendum through the governance process | W8 |
| O-21 | Settings sprawl: three structs + 9 loose fields + storage/RAM fields duplicating `DataLoader`'s; no shared config representation for bindings (§8 D10; design.md:62-64) | one config representation | DEFER to the interface campaign (all three languages); this campaign only stops the bleeding: no new loose fields, the CLI's parsed options become a `Config` aggregate the session consumes (W6) | W6 / next |
| O-22 | design.md says latency-bound, LESSONS/MEMORY say memory-bound; no PMU artefact (§5 note, Q7) | one stated model per regime | KEEP code; docs state both regimes (single-pair recurrence: latency-bound; large-N fill: memory-bound) and register a PMU measurement task | W8 |
| O-23 | 19 DO-NOT-BREAK structures whose rationale lives only in header comments (§5, Q10) | invariants recorded where a cleanup will look | KEEP all 19; promote the table into `design.md` §Invariants | W8 |
| O-24 | Move semantics repaired lazily via `dtw_binding_owner_` (P8) | value-capture removes `this` from closures | CHANGE (consequence of O-02): closures no longer capture `*this`, so a moved `Problem` needs no repair; `dtw_binding_owner_` and `repair_dtw_binding_after_relocation` retire with a use-after-move test kept | W4 |

## Algorithms + MIP (from `.claude/reports/2026-09-07-asis-algorithms-mip.md`)

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| A-01 | `Method` names 4 of 10 algorithms; Tier-1 keeps a separate 9-string table; `Problem::cluster()` can never reach FastPAM (§6 D1) | one enum, one string table at the surface | CHANGE: extend `Method` (additive) to every shipped algorithm; one `to_method(string)`/`to_string(Method)` pair in `enums/Method.hpp` used by Tier-1, CLI and bindings; `Problem::cluster()` dispatches on it | W3/W4 |
| A-02 | Three result-publication policies: raw non-transactional triple write (6 algorithms), no write (barycenter), validating transaction (4 MIP backends) (§6 D2) | algorithms return values; one publish path | CHANGE: every clustering algorithm returns `ClusteringResult` (barycenters return their own result type); `Problem` publishes through the one validating transaction. Also dissolves the three "run a heuristic unpublished" mechanisms (§5 D-G) | W3 |
| A-03 | Algorithms take `Problem&` (33 members; 8 exist only for MIP's nested heuristics); `fast_clara` constructs sub-`Problem`s (§6 D3, §10 Q1) | narrow `DistanceOracle` + `SeriesSource` + options | CHANGE per algorithm: template on an oracle concept (`size()`, `d(i,j)`, `row(i)` where cached); `Problem::oracle()` provides the view; CLARA's subsample becomes a sub-oracle over an index map (PAM on a sub-matrix), no `Problem` construction. Oracle: conformance + fingerprints + the `0af4cbd` precedent of byte-identical objects where the change is claimed no-op | W3 |
| A-04 | Eight non-composable option structs validated in five places; `BarycenterClusteringOptions` hand-copies `BarycenterOptions`; `LagrangianParams` 9/10 unreachable (§6 D4, Q5) | composition, one `validate()` per struct | CHANGE the composition and validation; DEFER reachability (exposing LR/CLARANS knobs is an interface decision for the all-languages campaign) | W3 / next |
| A-05 | CLARANS: unvalidated options publish an empty clustering (D5), `double→int` UB and cosmetic 64-bit fix (D11), no CLI/Tier-1 caller, header says "experimental, promote only after benchmarks" never run (Q7) | YAGNI, but do not break just to break | CHANGE: validate options, fix the UB, use the checked converter; keep the algorithm (bindings expose it); flag for the interface campaign's keep/delete decision with a benchmark | W3 |
| A-06 | `dist_by_ind` double preflight in the SWAP kernel, unmeasured (D6, Q4); IPO inlining of `dist_by_ind` unknown (Q10) | measure, then remove | CHANGE = O-01; W0 registers the N=2000 FastPAM1 sweep with/without the preflight and an IPO-on/off symbol check as baseline evidence | W0 → W4 |
| A-07 | Benders prints and returns on an invalid problem — the last silent-success path in MIP (D7) | typed failure | CHANGE: throw `InvalidInput` | W3 |
| A-08 | `MIPSettings::benders` and `PdlpParams::variant` are strings compared to literals (D8) | enums below the surface | CHANGE: `enum class BendersMode {Auto, On, Off}` etc.; string accepted only at CLI/binding edges (= O-11) | W3 |
| A-09 | `hierarchical` and `tadpole` publish `total_cost` via unguarded reassociable `+=` — NaN silently pins `best_idx` (D9, §5 D-H) | one numeric contract | CHANGE: route through `OrderedMedoidObjective` (accepting P6's volatile store) | W3 |
| A-10 | `int` truncation before range guards in hierarchical/tadpole (D10) | checked conversions | CHANGE: one `checked_point_count()` in base, used everywhere | W1/W3 |
| A-11 | Error taxonomy mixed (`runtime_error` vs `InvalidInput`) across clarans/hierarchical/medoid_utils (D12) | typed errors | CHANGE: `InvalidInput` throughout; keep the documented Gurobi handler order | W3 |
| A-12 | LR-core copies a `vector<int>` per B&B node (D13); `prepare_dense_D` doubles the N² matrix (D14) | no per-node allocation; read the packed matrix | CHANGE D13 (path array + depth). D14: read through the oracle's `row(i)`/packed access instead of a dense copy — after A-03; measure memory on the N=46 340 ceiling case | W3 |
| A-13 | `dtwc.hpp` exports `algorithms/detail/medoid_utils.hpp` and omits `tadpole.hpp` (D15) | umbrella = public surface | CHANGE | W3 |
| A-14 | `dtwc_medoid_assignment_failure` critical name shared by two TUs; LESSONS says "every critical distinct" (D16) | cold path, policy name | KEEP; clarify the lesson's wording | W8 |
| A-15 | Unreachable duplicate-centroid guards (D17), dead tie-break clauses in hierarchical, tautological `static_assert` in clarans (§7), overclaiming docs (D20) | YAGNI; docs true | CHANGE: delete/fix. `fast_pam`'s BUILD save/restore (D19) is a strong-exception-guarantee mechanism → KEEP, document, add the test | W3 |
| A-16 | `pruned_fraction()` over-reports after `tadpole_auto_dc` (D18, Q9) | one ledger per call | CHANGE: `auto_dc` counts through `exact()` | W3 |
| A-17 | Balinski model built three times with different layouts and cost orientations, equivalent only while D is symmetric (§5 D-F, Q3) | one model description | CHANGE: one `MipModel` builder (layout-free) consumed by the HiGHS/Gurobi adapters and the PDLP arbiter; precondition "D symmetric" stated and debug-asserted (true for all shipped costs, core DNB-2) | W3 |
| A-18 | Nearest-medoid scans ×7 kept apart by design (P7: runtime switches would enter the hottest loops) — vs. the ideal's single helper | zero-overhead helpers only | CHANGE only where a *template* helper (callables and index space as compile-time parameters) yields byte-identical object code (the `0af4cbd` test); start with clarans' two identical loops (D-C). ΔTD ×2 (D-E) KEEP | W3 |
| A-19 | k=1 / k=N special cases hand-coded at four sites; LR-core deliberately has none (D-I, Q11) | one precondition layer | CHANGE: trivial-k handled once at the publish layer (`Problem::cluster`/Tier-1); algorithms assert `1 < k < N` | W3 |
| A-20 | `barycenter` keeps its own DTW DP because it needs the alignment path (D-J, Q8) | one kernel family | DEFER: a path-returning core kernel is a design addition; KEEP the duplicate, documented | note |
| A-21 | Process-global `randGenerator` consumed by unseeded `fast_pam`, exported to both bindings → data race and cross-call dependence (§8 G4) | no shared mutable engine | CHANGE: unseeded entry points draw one seed from a thread-safe source and call the seeded path; the global engine stays only for the documented legacy Tier-2 `init::random` | W3 |
| A-22 | `one_batch_pam` holds a process-wide `static std::mutex` to print one warning (§8 G6) | no locks, no I/O in core | CHANGE: the warning becomes a `OneBatchPAMStats` field / verbose callback | W3 |
| A-23 | Lazy `dist_by_ind` priming re-implemented in tadpole, initialisation and `Problem` (§8 G2) | one seam | CHANGE: dissolves with O-08 | W4 |
| A-24 | Hidden `[.]` floors: TADPole ≥50 % prune, FastPAM1 identity, LR/PDLP bands (§7) | registered bands run or are deleted | CHANGE (tests wave): promote each to a default-run test with a machine-independent band, or delete with the reason recorded | W7 |
| A-25 | 16 DO-NOT-BREAK items (§4) incl. the falsified-speed-claims set (P16) | invariants recorded | KEEP; promote into `design.md`; the campaign never re-asserts a killed claim | W8 |

## IO + CLI + build (from `.claude/reports/2026-09-07-asis-io-cli-build.md`)

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| B-01 | `run_cli_main` is 1145 lines: declaration, config merge, validation, routing, dispatch, output (§7 P1) | thin CLI: parse → config → session → writers | CHANGE: parse into a `CliConfig` value; `InputKind` resolved once; load / run / write as separate functions. Oracle: the four real-binary integration tests + `test_cli_config_formats` byte-identical | W6 |
| B-02 | Twelve string options re-parsed into enums by hand; a post-parse alias remap duplicates CLI11's transformer maps and must be kept in sync (P2, §6 D8) | enums at the surface, once | CHANGE: `CheckedTransformer` straight into the enums; delete the remap | W6 |
| B-03 | Validation inconsistent (`--band`, `--max-iter`, `--dc`, `--mip-gap`, … unvalidated; `--seed` range a no-op) (P3) | validate at the boundary | CHANGE: `CLI::Range` wherever a range is documented | W6 |
| B-04 | Payload-load if/else chain spliced by the preprocessor — the chain's head differs per build flavour (P4, Q4); unknown extensions incl. `.h5` fall into the CSV parser while README advertises HDF5 (P5, O10, Q2) | one `switch` with every branch present; loud rejection | CHANGE: `InputKind` switch, unbuilt kinds throw the built-without message; `.h5/.hdf5` rejected with the conversion hint; README corrected | W6 / W8 |
| B-05 | Every CLI output stream is open-checked, never close-checked — full disk ⇒ truncated labels and exit 0 (P6, H); `create_directories` unchecked (P7) | one checked writer | CHANGE: = O-09 (one `io::artefacts` module with `open_output`/`close_output`) | W5 |
| B-06 | `.names` sidecar read with no length check ⇒ empty names in artefacts (P8); truncated `--dist-matrix` leaves a partially populated matrix and the run continues (P9, Q6) | fail loudly, never half-state | CHANGE: reject a short sidecar; matrix import reads into a temporary and swaps atomically, else the run is fatal | W5 / W6 |
| B-07 | `main.cpp` depends on a repo-relative path (non-negotiable #1); README shows arguments it cannot take; example configs hard-code `data/dummy` (P10–P12, Q1) | no runtime repo-relative paths | CHANGE: `dtwc_main` takes the path as `argv[1]` (kept as the tutorial); docs and examples fixed | W6 / W8 |
| B-08 | Arrow IPC reader: `assert` as the only Release bounds guard (P13), `name` column `static_cast` to `StringArray` unchecked (P14, H), unguarded `stoul` (P15) | validate untrusted input | CHANGE | W5 |
| B-09 | nanoarrow ingest checks nulls and dispatches type per element, against the per-chunk invariant the Parquet path documents (P16); stream ingest leaks a batch on failure and discards names (P17) | one policy | CHANGE: hoist null-count and type dispatch out of the loop; release-before-throw; carry names | W5 |
| B-10 | Non-const path refs (P18); header-skip loop ignores `getline` failure (P19) | const-correct, loud | CHANGE small | W5 |
| B-11 | Duplication: Arrow status checkers ×3 (D1), eager Parquet residual (D2), CSV loop ×3 + open/close triple ×3 (D3, D4), extension scan ×2 (D7), `saturating_add` ×2 (D9) | DRY | CHANGE: mechanical, each in its file's wave; `saturating_*` move to base | W1 / W5 |
| B-12 | CMake: `option(DTWC_ENABLE_LLFIO)` declared inside a function (P20); dead `DTWC_HAS_MMAP`/`DTWC_HAS_PARQUET_LIB` writes (P21, P22, O2, O3); unconditional Gurobi probe incl. a `file(GLOB C:/gurobi*)` on every configure (P23); benchmark targets unguarded (P25); stale header inventory (P26 = C-24); sanitizer options bypassed by CI (P27); IPO failure names no escape hatch (P28); dead IWYU macro (O1); dead Gurobi legacy vars (O4); presets missing build/test twins (O15) | a build system that says what it does | CHANGE: one build-hygiene slice | W0 |
| B-13 | `SKIP_RETURN_CODE 4` blanket-on with 13 hand-written opt-out blocks using four different skip regexes (P29, P30, §6 D11); `get_target_property` ×5 (D12); disable-boilerplate ×4 (D13); F22 loops ×3 (D10); bench setup ×8 (D14); 12 preset assertions (D15) | one registration helper; skip tolerance opt-in | CHANGE: `dtwc_add_test(NAME … MARKER … [MAY_SKIP])` with one canonical skip regex; `dtwc_disable_optional()`; `dtwc_add_benchmark()`; table-driven preset checks | W0 |
| B-14 | 19 `DTWC_` vs 13 `dtwc_` option prefixes met on the command line (§4.1) | one prefix | DECIDE: rename to `DTWC_` with the old names accepted and warned for one release (user-visible) | W0 |
| B-15 | No CI leg builds with all optional deps OFF; `project_warnings` is empty in every CI build; no YAML-OFF, no hygiene/tidy/format job (P24, P31, Q8) | the non-negotiables are gated | CHANGE: add an all-OFF + `DTWC_ALLOW_SEQUENTIAL` leg, a `DTWC_DEV_MODE=ON` warnings leg, a YAML-OFF leg, and a lint job (`check_repo_hygiene.py`, clang-format check) | W0 |
| B-16 | `THIRD_PARTY_LICENSES.md` names only HiGHS; vendored nanoarrow (Apache-2.0) and the header-only deps are unattributed (P32, Q11) | redistribution compliance | CHANGE: enumerate every statically included component + nanoarrow NOTICE; the CPM disclaimer target stays for CPM packages | W8 |
| B-17 | Configure downloads CPM itself; llfio bootstrap `git clone`s quickcpplib and patches it (Q10) | reproducible configure | CHANGE: vendor `CPM.cmake` (standard practice, SHA-pinned). DEFER the llfio superbuild replacement (bigger; record) | W0 / next |
| B-18 | Doxygen: `PROJECT_NUMBER` stale (O13); `PREDEFINED` empty so every `#ifdef DTWC_HAS_*` reader is undocumented (O14); not a CMake target (Q14) | docs generated from the build | CHANGE: `docs` target, version from `VERSION`, `PREDEFINED` set, dot include graph published | W8 |
| B-19 | F22 apparatus (three probe OBJECT targets + 661-line script + fixture, MSVC-fragile) guards 33 contract-mandated shims | proportional evidence | DECIDE (aliases): keep the shims (contract, migration aid); collapse the apparatus to one compile-probe test per compiler family | W7 |
| B-20 | Stale comments/docs (O5–O12): vestigial `[[maybe_unused]]`, unused `<sstream>`, false "for resultsPath", "llfio (required)", contradictory YAML comments, README/example paths | comments true | CHANGE | W5 / W6 / W8 |
| B-21 | Whether `--checkpoint-interval` reaches the mid-fill save path is unverified end-to-end (Q13) | real-binary proof | CHANGE (tests): one real-binary test that kills/resumes mid-fill | W7 |
| B-22 | 16 DO-NOT-BREAK items (§5): metadata-first Parquet planning, per-chunk null/type dispatch, saturating RAM arithmetic, `.dtws` v1 layout, F14 preflight-before-truncate, `--mmap-threshold` exemptions, FP flag set ↔ EAP slack, IPO ↔ header-critical lint, `/openmp:experimental` PUBLIC, portable wheels | invariants recorded | KEEP; promote into `design.md` | W8 |

## Tests — flat `tests/unit` (from `.claude/reports/2026-09-07-tests-taxonomy-unit-flat.md`)

Corpus: 73 files, 28 819 lines, 894 cases, 3 148 assertion sites. Categories:
contract/oracle 31 files · capability-guard 9 · adversarial 9 · fingerprint 4 ·
integration 6 · mixed 4 · trivial 6 · implementation-detail 2 · perf-fence 2.
Per-file verdicts are in the report's §1 ledger; the rows below are the campaign
decisions.

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| T-01 | `unit_test_deterministic_series.cpp:318-349` asserts exact call counts *inside five sibling test files*, gated by F15's `PASS_REGULAR_EXPRESSION` — editing any GPU/MPI test breaks CTest (§3) | tests pin contracts, not sibling files | CHANGE FIRST in W7: replace the cross-file count assertion with a contract on the shared generator (the F15 portability property itself); record the F15 evidence it replaces | W7 (first task) |
| T-02 | ≈3 900 lines (13 %) across 8 GPU/Arrow files are dark in the canonical gate and score green via `SKIP_RETURN_CODE 4`; `test_cuda_correctness.cpp:129` traps a pure-host oracle inside the CUDA `#ifdef` (§3) | skips are opt-in and visible; host oracles run everywhere | CHANGE: host-side oracles move outside the `#ifdef`; the 8 files register with `MAY_SKIP` and a printed reason; the CUDA leg (`build/cuda-verify`) and the Arrow leg run them for real | W7 |
| T-03 | Delete/merge candidates with named cover: `unit_test_dtw_variants` → `test_dtw_mathematical_properties` (12/13; A6 case moves to `unit_test_wdtw`); `unit_test_scores_phase0` → scores adversarial + single-cluster; `unit_test_Problem.cpp:63-107` byte-identical to `unit_test_warping.cpp:40-58,187-210`; `unit_test_Problem_phase0:89-171`; `unit_test_warping_phase0:66-125`; `unit_test_deferred_allocation` → `wave2a_integration`; `unit_test_multivariate_data` → `unit_test_Data` + `core/unit_test_time_series`; `unit_test_benders` (5/7) → `unit_test_mip:553-610`; `unit_test_checkpoint:129-328` → `checkpoint_robustness:529-762`; `unit_test_scores_new` Dunn/inertia/NMI → scores adversarial (keep CH + ARI) (§1, 2 delete + 11 merge + 5 sub-file) | delete with coverage proof | CHANGE: each merge = one commit carrying the covering test's name and a before/after coverage diff of the contract; the 6 keep+tolerance files get the `WithinRel` form | W7 |
| T-04 | Slow: `unit_test_distance_matrix_properties` 116 s, `unit_test_clustering_algorithms` 57.5 s, `unit_test_checkpoint` 52 s, `unit_test_variant_distmat` 17.5 s, `unit_test_multivariate_dtw` 14.2 s, `unit_test_fileOperations` 11.3 s — all reducible without losing an assertion (§4) | small fixtures pin the same contract | CHANGE: shrink fixtures; keep one large-N case per file behind a registered band label (`perf`) | W7 |
| T-05 | Scaffolding copied: 15 temp-dir RAII helpers, 9 stdout/stderr captures, 2 `operator new` allocation probes, 4 CPU-DTW matrix wrappers, 5 one-off DP oracles (one dead), 21 `make_*_problem` builders, 6 local RNGs beside the mandated shared one (§2) | one `tests/support` library | CHANGE: `tests/support/{temp_dir, capture, allocation_guard, dtw_oracle, problem_builders}.hpp`; the dead oracle deleted | W7 |
| T-06 | 6 trivial files (1 052 lines, 92 assertions) and 2 implementation-detail files (762 lines) (§1 totals) | contracts only | CHANGE: rewrite as contract tests where the subject has none, delete otherwise (cover named per assertion) | W7 |
| T-07 | 72 files flat in `tests/unit`; names not matching subjects (§5) | layout mirrors code layers | CHANGE: move into `tests/{core,algorithms,mip,session,io,cli,api}` in the same commits as their merges; registration through `dtwc_add_test` | W7 |

## Tests — `tests/unit/{core,algorithms,mip,io,types}` (from `…-tests-taxonomy-unit-subdirs.md`)

Corpus: 40 files, 11 985 lines, 354 executed cases, 25 136 measured assertions, 313 s
(90 % in four MIP/pruned files). Verdicts: keep 27, keep+tolerance 1, merge 10,
rewrite 1, delete 1, tests-dead-code 1 file + 4 site groups.

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| T-08 | Adversarial siblings already cover the unit files: `unit_test_scratch_matrix` (delete; adversarial has every assertion plus the discriminator and thread_local cases), `unit_test_lower_bounds` (keep only A5 band<0 and F46 ragged envelope), `unit_test_fast_pam` (keep the 4096-seed D-sampling oracle and write-back), `unit_test_distance_matrix` (keep packed `raw()`/`to_full_matrix`) (§1 items 1–4) | one test per contract | CHANGE: merge with the named covering cases; each commit lists the assertions moved | W7 |
| T-09 | Same-contract pairs: `unit_test_problem_semantic_transactions` → `unit_test_variant_distmat`; `unit_test_variant_domains` + `unit_test_distance_semantics` → `unit_test_invalid_distance_enums` (one contract, one helper, one error constant); `unit_test_medoid_utils` → `unit_test_faster_pam:364-366`; `test_pruned_openmp_contract` → `unit_test_pruned_distance_matrix:597/:691`; `unit_test_Index` → `unit_test_Range` (§1 items 5–10) | DRY | CHANGE: merge | W7 |
| T-10 | `unit_test_dtw_api.cpp`: six cases compare a wrapper to the function it calls; `test_lagrangian_root.cpp:310` `REQUIRE(compared >= 0)` is vacuous (§3) | oracles are independent | CHANGE: rewrite `unit_test_dtw_api` as a contract test (or fold into the fill-vs-facade equivalence test of C-02); fix the vacuous assertion | W7 |
| T-11 | tests-dead-code: `unit_test_time_series.cpp` (whole file), `unit_test_dtw_api.cpp:30-39` (`lb_*_valid`, `L2Metric`), `unit_test_invalid_distance_enums.cpp:251-258/:291-298` (full-matrix `dtwMissing`/`dtwAROW`), `sync()` at 16 sites, `dtwFull` as oracle at 4 sites (§1) — correction to the core map: `.sync()` has 27 hits, all test/benchmark-only | dead code and its tests go together; oracles survive as test-side code | CHANGE: delete with C-09; the full-matrix `dtwMissing`/`dtwAROW` and `dtwFull` become `tests/support/dtw_oracle.hpp` before their library copies are removed | W2 + W7 |
| T-12 | Slow: `test_mip_backend_guards` 218 s for 48 assertions (a 40-solve seed search at `:353-384`), `test_lagrangian_root` 37 s, `unit_test_pruned_distance_matrix` 25 s, `test_reduced_cost_fixing` 11 s (§4) | fast by default, bands for the big cases | CHANGE: remove the seed search (pin the found seed), shrink fixtures, `perf` label for one large case each | W7 |
| T-13 | Scaffolding ×14 groups: brute-force p-median oracle ×3, MIP generators ×3, byte-identical 3-group fixture ×4, `InvalidInput` matcher ×5, temp-dir RAII ×10, stream capture ×11, allocation counter ×2, cost recompute ×5; `tests/support/deterministic_series.hpp` exists and no in-scope file uses it (§2) | one support library, used | CHANGE: fold into `tests/support` (= T-05); the deterministic generator becomes the mandated source in these files | W7 |
| T-14 | 4 trivial files (128 assertions) and 3 implementation-detail files (21 assertions) (§6) | contracts only | CHANGE: rewrite or delete with cover named | W7 |

## Tests — adversarial, integration, conformance, registration (from `…-tests-taxonomy-adversarial-integration.md`)

Corpus: adversarial 15 files / 9 605 lines / 334 cases / 1 034 assertions (contract 21
cases, adversarial 143, trivial-or-structure-only 158, vacuous/self-oracle 9);
integration 6 `.cmake` scripts (1 837 lines) + 3 `.cc` writers + 3 `.py` + 1 `.sh`;
4 conformance routes; registration `tests/CMakeLists.txt`.

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| T-15 | `cpp_conformance.cpp:212` regenerates the reference and then compares against it, writing into the source tree; `tests/test_util.hpp:23-26` ignores its template parameter and `:68` writes into the source tree (§4) | the reference is tracked, read-only, and pinned by hash | CHANGE FIRST in W0: the conformance route reads `conformance_reference.txt` only; regeneration is a separate, explicit script; a hash pin guards the reference (the F17 pattern). Until this lands, "conformance digit-identical" is not a gate | W0 |
| T-16 | 125 tests registered in the Arrow build, 11 carry a PASS regex; the other ~114 inherit `SKIP_RETURN_CODE 4` with no floor — incl. `cpp_conformance` and 12 of 15 adversarial files (§2) | skips opt-in; every test asserts it ran | CHANGE: `dtwc_add_test` defaults to no skip tolerance and an executed-assertion floor from the binary's own summary line; `MAY_SKIP` prints a reason; the four skip regexes become one; marker discipline = computed counters only (no hardcoded literals) | W0 / W7 |
| T-17 | Zero-assertion or vacuous cases: `test_arow_adversarial.cpp:619` (says so at L664), `test_lb_enhanced_webb.cpp:513` (prints "FALSIFIED" and exits 0 → covered by D3 `:924/:958`), `test_scores_adversarial.cpp:228` (assertions inside `if(!isnan)`), `test_lower_bounds_adversarial.cpp:506/:527` (test the file's own helper) and `:457/:479` (exact duplicates of `:768/:789`), `test_multivariate_adversarial.cpp:273` (asserts the loop ran) and `:670` (wall-clock `REQUIRE`, the reason for `RUN_SERIAL`), `test_scratch_matrix_adversarial.cpp:50` → `:27`, 18 identical AROW cases (§1) | every case has an independent oracle | CHANGE: delete/merge with the named cover; drop the wall-clock assertion (keep the correctness half); AROW cases become one table-driven case | W7 |
| T-18 | 158 of 334 adversarial cases are trivial-or-structure-only (§totals) | adversarial = a bug class with a witness | CHANGE: each surviving adversarial case names its bug class (LESSONS/CHANGELOG/commit) in its header; structure-only cases merge into the contract test of the same subject | W7 |
| T-19 | CLI-driving boilerplate repeated verbatim across all 6 `.cmake` scripts (`require_occurrences`/`require_no_skip`, work-root guards of two strengths, three `WORKING_DIRECTORY` choices) ≈ 300 lines; 12 scaffolding groups (`random_series` ×7, naive envelope oracle ×4, path enumerator ×3, full-matrix DP ×2, problem builders ×5) (§3) | one `tests/integration/common.cmake`; one support library | CHANGE: extract; the strong work-root guard becomes the only one | W7 |
| T-20 | 5 files (~1 332 lines) run in neither CTest nor CI; `stress_test_cli.sh` skips 2 of 3 phases on untracked data and prints "All tests passed" (§4) | everything tracked either runs or is deleted | CHANGE: register or delete each with the reason; the stress script fails when a phase is skipped | W7 |
| T-21 | Real-binary integration tests (9) are the only reachability proof for the CLI; none over 10 s (§1, §5) | keep; they are the W5/W6 gate | KEEP; extend with the mid-fill checkpoint resume test (B-21) | W7 |

## Backends (from `.claude/reports/2026-09-07-asis-backends.md`)

Host-side dependency is already narrow: the GPU backends receive `data_.p_vec`,
`band`, `cuda_settings.{device_id,precision}`, `verbose_`, `size()` and write through
`visit_distmat` + scalar `set`; no backend includes `Problem.hpp` or `Data.hpp`. MPI has
no `Problem` caller at all (benchmark + own test only). Metal is `[BLOCKED-ENV]` on
this machine (compile-only locally; macOS CI runs it). CUDA runs on the local RTX 4000 Ada.

| ID | Finding (source) | Ideal says | Verdict | Wave |
| --- | --- | --- | --- | --- |
| G-01 | Metal returns an all-zero matrix on scratch-allocation failure, with a comment claiming a CPU fallback that does not exist (§6 P1) | no silent fallbacks — typed `DeviceError` | CHANGE: throw; test on the macOS leg (alloc failure injected through the size guard) | W2b |
| G-02 | Metal's four wavefront kernels lack a zero-length guard → `cur[-1]` OOB `[inferred]` (P2) | validated inputs | CHANGE: guard at the host boundary (empty series rejected before launch, as CUDA does); regression test | W2b |
| G-03 | MPI truncates `local_count` to `int` — silent zeros above `INT_MAX` (P3); `k+w+1` envelope overflow at `INT_MAX` in both GPU backends (P4 = PLAN F50, open) | checked arithmetic | CHANGE: `checked_cast`/64-bit counts; coordinate with PLAN F50's owner so one fix closes both | W2b |
| G-04 | `Problem` hands the GPU only band + metric: `variant`, `missing_strategy`, `ndim>1` and `lb_strategy_` are silently ignored on the GPU route, with no guard anywhere (P5) | an unsupported combination is a loud error | CHANGE: the fill gateway rejects GPU strategy with any non-Standard/univariate/Error-strategy configuration (`InvalidInput` naming the axis); documented in the device guide | W4 |
| G-05 | Duplication: CUDA wavefront diagonal loop ×4, launch config ×2 (B4 partly fixed), warp/regtile kernels ×2 each, FP32/FP64 dispatch ×2, workspace structs ×2, upload/download inlined twice — causing a **double H2D transfer on the pruned path**; Metal wavefront body ×4; the INF sentinel is a literal in 12 places (§3 D1–D7, D9) | one kernel template per topology; one upload path; shared constants | CHANGE (CUDA, verifiable locally): template the wavefront loop on precision and band mode; one launch-config function; one upload/download path (fixes the double H2D — a measurable win); constants in `gpu_dtw_common.hpp`. Metal: same shape, compile-locally + macOS CI gate | W2b |
| G-06 | CUDA and Metal are not ports of each other (orientation, band clipping, FP64 support, kernel set differ) (D8); GPU kernels share no code with `dtw_kernel.hpp` (D9) | one semantics, separate implementations allowed | KEEP separate implementations (different execution models); CHANGE: one written semantics table (orientation, band clipping, precision) both are tested against through the same CPU oracle suite | W2b / W8 |
| G-07 | MPI duplicates the OpenMP fill shape but bypasses `dtw_fn_` (D10); MPI is unreachable from `Problem`/CLI/bindings | backends are fillers over the bound distance | DEFER wiring MPI into `DistanceMatrixStrategy` (feature, interface campaign); CHANGE now: the MPI fill consumes the same bound closure as the OpenMP fill so variants are not silently Standard-only | W2b |
| G-08 | No `test_cuda_*`/`test_metal_*`/`unit_test_mpi*` has a skip gate → a GPU-less host reports both suites green via SKIP (E7 open) (§9) | skips visible and floored | CHANGE: = T-02/T-16 (`MAY_SKIP` with reason; CUDA leg runs them for real) | W7 |
| G-09 | Untested: persistent mode, pinned-alloc failure, the >2048 three-buffer route, `require_shared_mem_fits` firing, Metal alloc-failure/zero-length paths, MPI multi-rank in CI, every GPU × non-Standard-variant combination (§9) | every route has a test or a recorded `[BLOCKED-ENV]` | CHANGE: add the locally runnable ones (RTX; CUDA-OFF seams for guards); G-04 closes the variant matrix by rejection; record the rest `[BLOCKED-ENV]` with the probe | W7 |
| G-10 | Two H100 records that must not be conflated: the UCR end-to-end run (`benchmarks/ucr_benchmark_results.json`, `docs/content/benchmarks/ucr.md:36`, 3.1 min / 14.2×, ARC 2026-04-09) has an artefact; the *kernel-level* sm_90 bands do not (`.claude/baselines/2026-07-07-cuda-first-runtime-verification.md:28`: "H100 (sm_90) perf claims remain ADVISORY until a real ARC run"); the only local hardware baseline is the RTX 4000 Ada (§4) | every speed claim names its artefact | CHANGE (docs): the benchmarks page cites the JSON artefact next to the H100 rows; kernel-level pages say "advisory" until a kernel run-log exists on sm_90 | W8 |
| G-11 | 12 DO-NOT-BREAK items: hoisted `row_start` + clamp ordering in `decode_pair`, lock-free `query_gpu_config`, atomic warn-once, thread_local growing workspaces, 256 KiB pinned threshold, on-device decode, direct N×N write-back, the `max_L>2048` three-buffer cap, Metal's 5e9-cell watchdog chunking and scratch-serialising waits, coalesced banded-row layout, MPI `MPI_IN_PLACE` INT_MAX chunking (§4) | invariants recorded | KEEP; promote into `design.md` | W8 |
