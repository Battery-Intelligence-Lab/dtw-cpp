# Handoff — 2026-07-23 — R2-D1 DTW recurrence and Sakoe-Chiba window

## Outcome

- Base `be0069b` was falsified by the preregistered unequal-length oracle:
  inherited CPU band 0 returned 8 instead of no path, and band 2 returned 3
  instead of 5. Both values matched the old endpoint-scaled corridor.
- The shared CPU band kernel now implements the fixed adjustment window
  `|i-j| <= band`, rejects an endpoint gap wider than the band, and computes
  its row ranges in overflow-safe `size_t`.
- Scalar, dependent-MV, independent-MV, singleton, missing-data, ADTW, WDTW,
  and AROW bypasses use the same feasibility contract. `INT_MAX` no longer
  enters signed `band+1` arithmetic.
- A full-matrix DP oracle and explicit enumeration of 696/1,143/1,289
  admissible paths independently reproduce the registered L1 and
  squared-local-cost ledger.
- Deterministic witnesses now pin that both accumulated forms are
  dissimilarities rather than metrics.

## Decisive evidence

The complete record, bands, red observations, focused outputs, and final
verbatim CTest summary are in
`.claude/baselines/2026-07-23-r2-d1-dtw.md`.

Final canonical gate:

```text
100% tests passed, 0 tests failed out of 114
```

Exactly six registered capability tests skipped: CUDA correctness, CUDA
LB_Keogh, Arrow I/O readers, and the three Metal tests. The inherited
113-target floor was stale; no target was dropped, and the stricter 114-target
suite passed.

## Open discrepancy

F12 remains open and is now sharper:

- CUDA still implements endpoint-scaled slope/window geometry and signed
  `band+1` expressions.
- Metal geometry is fixed-diagonal, but a no-path Float32 `FLT_MAX` is widened
  to double rather than mapped to CPU `DBL_MAX`.

The CPU gate is not evidence for either backend. Real CUDA execution is
required on `build/cuda-verify`; Metal remains environment-gated.

## Exact resume point

Commit the green D1 code/finding atomically. Then create
`docs/derivations/01-dtw-recurrence-sakoe-chiba.md`, update the derivation
index, synchronize `docs/content/method/dtw.md`, and verify the 1978 primary
source in `.claude/CITATIONS.md`. After those documentation gates pass, make a
separate PLAN bookkeeping commit that registers the CPU defect as the next R3
finding, updates F12 without claiming backend closure, and marks R2-D1
complete.

Rollback for the behavior change is `git revert` of the forthcoming single D1
code commit. The claim most likely to fail next is exact cross-backend sentinel
parity, not the repaired CPU recurrence.
