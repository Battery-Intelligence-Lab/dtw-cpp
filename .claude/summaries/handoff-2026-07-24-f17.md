# Handoff - 2026-07-24 - F17 real CLI resume

## Accomplishments

- Audited the real `dtwc_cl` binary-result producer/consumer flow at clean base
  `db129e5032614d2272ebd10e426126fb0cfb3a4d`.
- Confirmed `--resume` loads a complete `ClusteringResult`, discards it, runs
  clustering, and overwrites the source checkpoint.
- Confirmed dense directory checkpoint and mmap cache resume independently of
  `--resume`.
- Rejected generic algorithm continuation because binary v1 lacks every
  method-specific continuation state and is written only after a method
  returns.
- Registered completed-result replay semantics, a coherent non-degenerate
  checkpoint, real-CLI output discriminators, loud-error cases, preservation
  checks, twelve mutations, full-suite floors, and the two-attempt cap in
  `.claude/baselines/2026-07-24-f17-cli-resume.md`.
- Strengthened the positive gate before its first decisive run: one replay
  requests `kmedoids` and imports the fresh matrix so exact silhouette values
  prove method-generic replay plus `Problem` rehydration; the plain replay pins
  the exact three-file inventory; fixture timestamps are moved back 24 hours so
  an identical rewrite is observable.
- Ran the registered inherited gate. The validator passed 157 assertions in 26
  cases; the real CLI failed after printing `Loaded checkpoint: 41 iterations,
  cost=1650`, then `Running FastPAM (k=3)` and publishing cost 976 / iteration
  1. The exact red is recorded in the F17 baseline.
- Implementation attempt 1 satisfied every internal F17 assertion and printed
  the exact registered marker, but CTest's broad skip regex matched the marker
  field `algorithm_skipped`. The formal attempt is FALSIFIED. Attempt 2 changes
  only that redundant outer detector to a line-oriented skip diagnostic; every
  child output remains guarded by the script.
- Retained attempt 2 in product commit `fb853eb`. The real CLI passes the exact
  12-run marker, the validator passes 157 assertions / 26 cases, binary
  round-trip passes 9 / 1, documentation generation/contract checks pass, and
  the repaired SLURM script passes `bash -n`.
- Executed, killed, restored, and reran all twelve registered mutations. The
  gate detects fresh fallback, clustering after replay, a rewrite timestamp,
  verbose-only markers, all seven structural-validator omissions, and failed
  load fallback.
- Passed the three full C++ gates at `fb853eb`: canonical 120/120 with six
  capability skips, llfio-OFF 120/120 with nine, and Arrow-ON 122/122 with
  eight. The first canonical wrapper invocation timed out and was discarded;
  two complete long-timeout reruns passed.
- FALSIFIED the registered supply-chain sub-band after the product commit:
  62 tests passed and one failed because the new tracked real-CLI `.cmake`
  driver raises the index-owned manifest inventory from 27 to 28. The
  two-attempt cap forbids a third F17 rescue. F39 owns a separately registered
  reconciliation; the executable script must not be hidden behind another
  extension.
- Confirmed the SLURM checkpoint test is a false-green for binary resume: its
  second run selects a different output directory, so it cannot see run 1's
  `<name>_checkpoint.bin`, and identical deterministic labels prove only a fresh
  rerun plus the separate directory checkpoint. The repaired script copies the
  product binary into run 2, requires the replay marker and absence of FastPAM,
  compares cost/iterations/convergence/labels/medoids, and proves the source
  hash and timestamp unchanged. This is syntax-verified locally; no SLURM
  runtime is claimed.

## Decisions

- `--resume` replays a completed or iteration-capped result exactly and skips
  clustering; it is not a warm start and does not add `--max-iter`.
- Missing, malformed, or structurally incompatible explicitly requested binary
  state fails loudly.
- Replay never rewrites the source binary checkpoint.
- F17 preserves binary v1 and documents that the caller must use the same input
  order and clustering configuration. Authenticated provenance remains outside
  this frozen-format repair.
- Streamed FastCLARA continuation remains R5/2.1 work.
- Binary parser hardening, conflicting CLI/storage combinations, the mmap
  open-only deletion race, and the quadratic duplicate-medoid validator remain
  reviewed residuals under the existing checkpoint/config robustness lens.
- Keep F17 unchecked as formal falsification evidence. `fb853eb` is retained;
  F39 alone owns the manifest reconciliation, and F18 is the active campaign
  pointer.

## Exact resume point

1. Do not tune F17 or change its frozen 27-manifest band: both implementation
   attempts are spent and the formal result is recorded.
2. Start F18 from clean post-bookkeeping HEAD. Read the current MATLAB
   estimator/MEX route and killed-idea record, then register non-degenerate
   metric and real-CUDA dispatch discriminators before changing code.
3. Leave the 28-manifest supply-chain failure for F39's separately registered
   repair; do not rename the executable CMake driver to evade scanning.

The claim most likely to be wrong is the v1 provenance precondition. Structural
validation cannot prove that a same-N/k result belongs to the current data.
