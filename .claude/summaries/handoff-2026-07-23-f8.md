# Handoff — 2026-07-23 — R3-F8 FastCLARA resident/stream parity

## Outcome

- F8's inherited coverage claim was red: neither the tracked fixture nor a
  named parity CTest existed.
- The tracked 1,451-byte, four-row-group fixture is SHA-pinned and contains
  eight synthetic four-sample series.
- A conditional, non-skippable CTest now drives the fresh real `dtwc_cl` six
  times: Float64 Standard, Float32 Standard, and Float64 Soft-DTW, each in
  resident and 900-byte forced-stream modes.
- Live route markers pass 12/12; all 18 artifacts exist; all nine
  resident/stream pairs are byte-identical; labels, medoids, 72-byte
  checkpoints, exact cost bits, and three distinct configurations are pinned.
- The Ubuntu Arrow job selects F8 explicitly with `--no-tests=error` and checks
  the execution marker, so omission or skipping cannot pass.

## Decisive evidence

Focused real-binary marker:

```text
F8_PARITY subject=real_dtwc_cl runs=6 route_markers=12/12 parity=9/9 configs=3/3_distinct fixture_sha256=2F259F418A6BB9C62CA0004CB334C05E8309213F5A76DC890F83C15D5BDA3CA8
```

The fresh Arrow reader gate executed 390 assertions in 11 cases with no skip.
The complete Arrow-ON suite passed 115/115; the canonical Arrow-OFF suite
remained 114/114 with exactly its six registered capability skips. Full
commands, hashes, output, assumptions, and the pre-run decimal correction are
in `.claude/baselines/2026-07-23-f8-fast-clara-parity.md`.

## Exact resume point

F8 is closed by fixture/test commit `1df77fc` and the separate PLAN
bookkeeping update. Resume at the earliest open R3 finding, F11, unless an R2
derivation is deliberately interleaved.

Rollback is `git revert 1df77fc`.
The claim most likely to fail next is cross-platform last-bit Soft-DTW cost
identity in the hosted Ubuntu Arrow job; local Windows parity and both complete
local gates are confirmed, but hosted CI remains an operator-side result.
