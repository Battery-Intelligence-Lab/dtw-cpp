# Handoff — Continuous PLAN campaign — 2026-08-09

## Campaign start

- **[confirmed]** The campaign resumed from
  `4a2d154e2a08b3f95f5735055e4bbd276b0c8693` on local branch `Claude`.
- **[confirmed]** `git status --porcelain=v1` printed no paths before this
  handoff was created; there were no inherited worktree changes to classify.
- **[confirmed]** The local branch was 67 commits ahead of `origin/Claude` at
  the opening probe. No remote operation is authorized or planned.
- Read `AGENTS.md`, `PLAN.md`, the current killed-ideas register,
  `.claude/LESSONS.md`, `.claude/CITATIONS.md`, `.claude/design.md`,
  `.claude/TODO.md`, the archive's D3 and explicit-rejection records, and the
  preceding D3 handoff before changing the repository.
- The continuous objective is to implement and validate every remaining
  `PLAN.md` item, with one local conventional commit per task or finding and
  no subagents.

## Exact resume point

Close D3/F54/F55/F57 exactly as registered: rebuild the canonical tree, run
the inherited focused D3 subjects and both non-skippable D3 targets, then run
the canonical, llfio-OFF, and Arrow-ON matrices serially. Adjudicate the
prospective 125/125, 125/125, and 127/127 floors with exact 6/9/8 capability
skips, the Arrow reader's 390 assertions/11 cases, and all four real-CLI
markers. Only then update documentation, `CHANGELOG.md`, `AGENTS.md`, PLAN
checkboxes, the decisive run-log, and repository hygiene records.

The claim most likely to be wrong remains the preceding handoff's claim:
exact clipped-tail equivalence between the production recurrence and the
direct-predicate NoLR oracle. The registered exhaustive ledger is the arbiter;
its bands must not move.

## Rollback

This opening checkpoint is documentation-only and has no product/runtime
effect. Its rollback is the single local commit that adds this file. No remote,
published, tagged, data, or operator state has changed.
