# Handoff - 2026-07-24 - F13 medoid assignment contract

## Accomplishments

- Read PLAN.md, the killed-ideas record, the archived plan, and
  `.claude/LESSONS.md` before F13 work.
- Audited every named FastPAM, CLARANS, and FastCLARA assignment body.
- Found six algorithm bodies rather than the stale “four copies” model, plus
  the separately reachable Lloyd `Problem::assign_clusters` body.
- Confirmed first-slot tie behavior, divergent objective reductions, absent
  non-finite checks, the finite-`DBL_MAX` best-result collision, and the
  separate CPU-f32 widened-`FLT_MAX` public-boundary defect.
- Registered independent literal oracles, live public routes, exact messages,
  exact IEEE-754 objective bits, real Arrow CLI routes, assertion/count
  floors, mutation requirements, and the two-attempt cap in
  `.claude/baselines/2026-07-24-f13-medoid-assignment-contract.md`.
- Used header-only exploratory probes to pin portable-v1 seeds. These probes
  are not decisive evidence.
- Added the CPU-f32 public-boundary regression. The inherited live resolver
  failed 1/1 assertion by returning widened `FLT_MAX`.
- Repair attempt 1 is committed in `62c6f26`: every CPU resolver route and the
  existing GPU seam now share exact compute/public sentinel normalization.
  The focused repair passes 5/5 assertions, the GPU host contract passes 8/8,
  and the complete distance-semantics target passes 53/53.
- Independent audit caught that the finite Arrow parity fixture could not kill
  validation removal in either streamed scan. The preregistration now requires
  two generated build-local poison fixtures, exact f64/f32 streamed
  diagnostics, and `stream_rejections=2/2`; no tracked data may change.
- The first 65-row/one-group/900-byte discriminator was FALSIFIED before
  assignment because selected-row loading required 4680 bytes plus 112 bytes
  retained. The second/final registered topology is 129 rows in groups 65+64
  with a 5000-byte cap; exploratory f64/f32 runs both reached point 0, slot 0,
  medoid index 65 through the real streamed CLI.
- Assignment repair attempt 1 passed the focused and strengthened Arrow gates
  but the canonical full suite was FALSIFIED at 115/116 by a Tier-1 test that
  explicitly required Lloyd to publish an infinite objective. Repair attempt
  2 retains the registered rejection policy and updates only that stale test
  to require `InvalidInput` plus unchanged pre-call labels; its focused CTest
  now passes 1/1.
- Assignment repair attempt 2 is committed as `1eb8609`. The final focused
  target passes 114 assertions / 8 cases; canonical and llfio-OFF pass 116/116;
  the Arrow build passes 118/118, including both real-CLI targets.
- Diagnosed the apparent Arrow unit-test hang as a pre-main Windows loader
  failure. Without PyArrow runtime directories the executable exits
  `-1073741515` (`0xC0000135`); with LLVM, `pyarrow`, and `pyarrow.libs`
  prepended it passes 841 assertions / 21 cases in 0.20 s. The full Arrow gate
  passes under that environment. A repository-side CTest environment repair
  remains a separate finding.
- Built and loaded a fresh current Python extension with matching SHA-256
  `5139A674...BF50`. The corrected-runtime full suite is FALSIFIED at
  3 failed / 1007 passed / 12 skipped.
- Built a distinct extension from exact base `1af0aa8` in the retained
  `build/f13-base-arbiter-1af0aa85-20260724a` worktree. Isolated provenance and
  SHA-256 `25F8D6C5...BEF5` were asserted; the same three tests failed at base.
  The stale portable-RNG Python expectations are therefore confirmed
  pre-existing.
- Registered a test-only repair before editing: exact portable-v1
  medoids/labels/cost for the default-seed cases and a simpler six-singleton
  fixture that still distinguishes `max_iter=1` from convergence. The current
  Python floor is 1,022 collected, not the stale 418-test handoff floor.
- The repaired three-node Python gate passes, and the live `max_iter=100`
  forwarding mutant fails on actual medoids `[5,1]` versus registered capped
  `[4,1]`. Commit `cb11c90` retains the repaired test.
- The next full Python run was FALSIFIED at 1 failed / 1009 passed / 12 skipped:
  F13 added one tracked `.cmake` gate but the supply-chain index inventory
  still required 25. Exact `git ls-files` comparison against `e37b71a` proves
  the sole addition is
  `tests/integration/test_fast_clara_assignment_contract.cmake`; a two-literal
  25-to-26 repair is registered before editing.

## Decisions

- Include Lloyd assignment in F13 so the audit finding is not silently
  omitted.
- Keep full scan consolidation in R4. F13 may share only finite-state and
  ordered-objective policy needed by the behavioral repair.
- Repair CPU-f32 public no-path translation in its own implementation commit.
- Preserve negative finite Soft-DTW distances, exact finite `DBL_MAX`, the
  first medoid slot on ties, and FastCLARA's parent-cache independence.
- Reuse the tracked read-only F8 Parquet fixture for the streaming tie gate;
  do not add or modify a data file.
- Generate the streamed non-finite discriminator only inside the test-owned
  build work root through an Arrow-linked helper.
- Retain the second streamed-discriminator topology; the two-attempt cap
  forbids further fixture rescue-tuning.
- Treat the inherited Lloyd-infinity test as a contradictory expectation, not
  an exception to the F13 finite-state contract. Repair attempt 2 may update
  the test but may not weaken production rejection.
- Repair the stale Python Lloyd literals without touching production. Do not
  update the old cap fixture to equal capped/default values: use the registered
  `[0,1,2,3,5,4]` singleton fixture so the forwarding test retains teeth.
- Treat Windows Arrow CTest runtime discovery as an independent infrastructure
  finding. Its fix must apply to all affected Arrow-linked tests, not special
  case `unit_test_fast_clara`.

## Exact resume point

1. Implement only the registered Python test-oracle repair and run its focused
   nodes plus the forwarding mutation. **Done in `cb11c90`.**
2. Apply the registered supply-chain manifest-count increment, run its live
   checker/focused mutation, then run the full fresh-extension Python gate;
   required current floor is
   1010 passed / 12 skipped / 0 failed.
3. Register and repair the Windows shared-Arrow CTest runtime environment as a
   separate finding, then prove ordinary CTest works without a caller PATH.
4. Record mutation transcripts and final gate outputs verbatim, close F13 in
   PLAN.md, update the handoff to resume at F14, and commit bookkeeping alone.
