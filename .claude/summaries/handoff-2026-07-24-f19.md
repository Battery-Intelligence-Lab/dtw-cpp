# Handoff - 2026-07-24 - F19 Problem encapsulation

## Accomplishments

- Started from clean F18 closeout `926542f`.
- Read the live PLAN, frozen API contract, original `c8c74bd` freeze, archive,
  LESSONS, F22 ownership, and M25/M37 binding decisions before choosing scope.
- Completed independent C++ and MATLAB read-only audits.
- Confirmed `resize()` is already private despite the current documentation.
- Resolved the contract conflict to an exact 10-private / 11-retained field
  split without consuming F20, F22, or the legacy semantic-cache guarantees.
- Inventoried the sole MATLAB helper and its exact four call sites; confirmed
  all four corresponding core algorithms already publish k/labels/medoids.
- Registered a non-degenerate eight-series L1 fixture. Independent dynamic
  programming produces 28 distinct pair distances; exhaustive medoid
  enumeration gives unique optimum `{2,7}` at cost 87, next-best 89.
- Registered the 31-assertion compile contract, source/API guard, focused C++
  runtime, exactly 24 MATLAB route executions across R2024b/R2025b, twelve
  permanent mutations, binding/full gates, and a two-attempt product cap.
- Committed the registration and binding decision as `83472ab`.
- An adversarial review rejected the first executable-gate draft before
  commit: it could reuse a stale MEX, did not aggregate the fixed 24-run
  schedule, omitted three registered source mutations, admitted mutable
  accessor overloads, accepted an unrelated compile failure, and carried the
  obsolete 407/11 Python floor. No product attempt was consumed.
- Recorded the clean inherited canonical baseline at tracked base `03f0e61`:
  build reported `ninja: no work to do.`, CTest discovered 120 tests, zero
  failed, and exactly the registered six capability tests skipped.
- Committed the exact-diagnostic expected-red C++ gate as `7e4c03e`.
- Ran the inherited 1,022-test Python collection against the installed
  extension: `1 failed, 1010 passed, 11 skipped`. The sole failure is F39's
  already-open tracked-CMake inventory mismatch, observed 28 versus stale 27.
  The extension was not rebuilt for this inherited probe; the final B1 gate
  still requires a fresh build and an F19-specific symbol check.
- Committed the fixed six-profile MATLAB capture/oracle machinery in
  `40f2efd` and the 31-assertion/27-probe C++ source gate in `27a2561`.
- Captured six clean-first, source-distinct, MEX-distinct MATLAB profiles. The
  inherited, four single-call deletion, and composite profiles all carry
  source/MEX/build-log/manifest hashes.
- The first aggregate MATLAB schedule exposed PowerShell singleton unrolling
  after the inherited profile. Commit `f4613d6` adds a typed route selector,
  five-case regression, and four legacy-mutant rejections.
- Completed the final schedule
  `schedule-20260724T115627977Z-c3516137`: 6/6 profiles, 12/12 release runs,
  24/24 route executions, 192/192 vector and 120/120 scalar assertions,
  24/24 MEX hash checks, 42/42 post-run rehashes, and zero skips.
- Restored `bindings/matlab/dtwc_mex.cpp` byte-identically to inherited SHA-256
  `DDF29F995A2CC6FAF1CCD8DF2ABB3FCFE5E34643C49E6CD08A9AE0236A1378F3`.
- Reran the clean expected-red boundary: 27/27 adversarial probes and the
  inherited 31-assertion profile pass; the final profile rejects exactly the
  ten registered raw public fields. No product attempt has been consumed.

## Decisions

- Privatize exactly `method`, `random_seed`, `last_iterations`, `tadpole_dc`,
  `lb_strategy`, `storage_policy`, `verbose`, `output_folder`, `name`, and
  `data`.
- Retain public `maxIter`, `N_repetition`, `band`, `variant_params`,
  `missing_strategy`, `distance_strategy`, `cuda_settings`, `mip_settings`,
  `init_fun`, `clusters_ind`, and `centroids_ind`.
- Preserve Python's writable name/output/config properties through setters;
  bindings may not address or assign private state.
- Keep `last_iterations` read-only.
- Treat the four single-call deletions and composite MATLAB deletion as
  deliberate mutation tests, not implementation attempts.
- A MATLAB route selector must return a typed array even for one route;
  branch-local `@($scalar)` does not survive `if` pipeline unrolling.
- Byte-exact mutation of the mixed-EOL MATLAB source uses the captured strict
  UTF-8 snapshots and hash checks; text patching is not identity evidence.
- Do not claim the interrupted canonical parent process as a clean full-suite
  run. Its complete log has 114 ordinary passes, six capability pass reasons,
  and zero `Test Failed` records; the stale final-test ledger was rerun with
  actual exit 0. A fresh full suite remains a final F19 gate.

## Exact resume point

1. Begin product attempt 1 with the registered ten backing fields and exact
   getter/setter signatures; keep the eleven compatibility fields public.
2. Migrate core callers. Keep Benders warm-start restoration inside
   `Problem` friend context without adding a public `last_iterations` setter,
   and make the float-conversion path consume `const Data&`.
3. Migrate Python and MATLAB bindings through the public API, then remove the
   redundant MATLAB helper and its four calls.
4. Migrate focused/native tests without exposing mutable `data` or a test-only
   public result mutator. Add the registered non-degenerate C++ runtime gate.
5. Run the final encapsulation/source profile before broad builds. Product work
   remains capped at two attempts.
6. Commit the product and tests once focused gates are green, then run fresh
   canonical, llfio-OFF, Python-extension, and MATLAB gates. Commit generated
   docs and closeout bookkeeping separately.

The claim most likely to be wrong is the interpretation of generic F19 privacy.
An all-field interpretation conflicts with the original freeze and still-
binding compatibility decisions; any broader change needs an explicit new
break, not an implicit refactor.
