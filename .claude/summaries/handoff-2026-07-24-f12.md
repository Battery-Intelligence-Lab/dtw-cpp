# Handoff - 2026-07-24 - F12 GPU fixed-band parity

## Accomplishments

- Audited CUDA, Metal, existing GPU tests, D1 evidence, killed ideas, and
  binding lessons from base `e529127`.
- At the inherited base, confirmed six CUDA geometry copies used the
  endpoint-scaled corridor and signed `band+1`.
- At the inherited base, confirmed CUDA FP32 and Metal FP32 result copies
  widened `FLT_MAX` instead of returning the public double `DBL_MAX` no-path
  sentinel.
- Registered the exact unequal-length oracle, all CUDA kernel routes,
  overflow cases, executable assertion floors, and the two-attempt cap in
  `.claude/baselines/2026-07-24-f12-gpu-fixed-band-parity.md`.
- Probed the local Metal environment and recorded `[BLOCKED-ENV]`; permanent
  source/tests continue, but real-device closure is not claimed.
- Freshly rebuilt the inherited CUDA correctness binary and ran it on the RTX:
  7,277/7,278 assertions passed; the one failure was the existing
  unequal-length case returning finite `87.32076646498155981` against the
  canonical CPU `DBL_MAX` result. Verbatim output is in the F12 run-log.
- Added the permanent independent oracle and CUDA/Metal route tests. The first
  focused real-RTX run passed its oracle case and failed all five CUDA subjects
  with the registered fingerprints (59/64 assertions passing before Catch2
  stopped each failed case).
- Repair attempt 1 is retained in `4583443`: one ordered-subtraction predicate
  now implements canonical geometry in all six CUDA kernels; obsolete boundary
  arrays were removed from shared-memory sizing; CUDA and Metal FP32 public
  result copies translate exact `FLT_MAX` to exact `DBL_MAX`; Metal's no-LB
  wavefront bounds and host route-selection multiplication are widened.
- An independent review found the first green test missed the registered
  reverse singleton external-query branch. The immutable test was completed,
  rebuilt, and the final real-RTX gate passed 515 assertions / 6 F12 cases.
  The unfiltered CUDA binary passed 7,827 assertions / 61 cases with no skip,
  and the CUDA correctness/LB CTest pair passed 2/2.
- The host sentinel-normalizer gate passed 8/8 assertions. The final canonical
  gate passed 115/115 with exactly six capability skips; the final llfio-OFF
  gate passed 115/115 with its nine expected capability skips.
- Three independent post-fix reviews found no remaining CUDA or source-level
  Metal blocker. The fresh Metal-OFF binary executed zero assertions and
  returned skip code 4, so F12 remains open pending a real Apple build/device.

## Decisions

- Fix all six CUDA kernels, including one/K-vs-N, in one behavioral task.
- Treat the double-valued result sentinel as backend-independent: device
  `FLT_MAX` is translated to exact public `DBL_MAX`.
- Use the D1 full-matrix oracle plus explicit path enumeration; production CPU
  DTW is not the arbiter.
- Keep F12 open after local CUDA closure if the real-Metal gate remains
  unavailable.
- Keep Metal LB integer arithmetic outside this no-LB task; F28-F30 retain that
  finding. No real-Metal behavior is inferred from the Windows source build.

## Exact resume point

1. Do not reopen F12 locally. Its locally executable bands are complete at
   implementation commit `4583443`; resume it only with an Apple
   Objective-C++ build and real-Metal run of `test_metal_correctness` that
   prints no skip and executes at least 100 assertions.
2. Continue the campaign at F13. Register adversarial nearest-medoid tie,
   objective, and non-finite bands before changing any duplicated assignment
   scan.
