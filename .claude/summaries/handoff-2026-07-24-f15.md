# Handoff — 2026-07-24 — F15 deterministic test support

## Accomplishments

- Started from clean F14 closure `e79fab31a3807fe3e2a7a83c60a455b4da91b880`.
- Re-read F15, Killed ideas, the archived plan, R1's TODO verdict, LESSONS,
  TODO, design, citations, and the historical June audit claim.
- Confirmed the old eight-file byte-identity premise is false. Five benchmark
  scalar generators use fresh per-series `mt19937` engines and `[-1,1]`;
  three accelerator tests use one continuous row-major engine and `[-10,10]`.
- Inventoried adjacent float/range/shape/draw-order variants and independent
  mathematical oracles. They remain deliberately outside the shared default.
- Confirmed dense full/banded backend references may share only their
  zero-diagonal upper-triangle/mirror traversal. Packed LB, MPI timing,
  cross/row layouts, and independent DP/path/formula arbiters remain separate.
- Built a preflight under Clang Release relaxed flags and MSVC Release precise
  flags. Raw generator/oracle hashes are frozen in the baseline. Independent
  full-matrix DP and production rolling-buffer DTW were digit-identical in
  both profiles, and band 0 differs from full DTW.
- Pre-run portability review found the standard-library mapping is also part
  of the contract. WSL Ubuntu 24.04 GCC 13.3 and Clang 18.1 with libstdc++
  agreed on a third exact profile, now registered before attempt 1.
- Rebuilt six benchmark executables and recorded their registration counts.
- Rebuilt the CUDA subjects inside `vcvars64` and ran the RTX 4000 Ada for
  real: 7,827 assertions / 61 cases and 688 / 8, zero failures.
- Ran CPU accuracy/SIMD: 283 / 39 and 7,029 / 16. MPI-OFF prints its exact
  capability message and returns 0 in the unit executable; the benchmark
  prints its own exact capability message and returns 1.

## Decisions

- Create a new self-contained support header; do not extend the incompatible
  global-state/ragged-data `tests/test_util.hpp`.
- Share fixed benchmark scalar/per-row generation, fixed accelerator
  continuous generation, and generic dense symmetric traversal only.
- Preserve the legacy STL-distribution algorithm. This is not a migration to
  production `portable-v1`.
- Accept only one coherent preregistered compiler-plus-standard-library
  fingerprint: Windows Clang-relaxed, Windows MSVC-precise, or Linux
  libstdc++.
- Cap implementation at two attempts. All 13 registered mutation executions
  must fail before closure.
- Real Metal remains locally unavailable; no Metal runtime claim is required
  or made for this test-only refactor.
- Implementation attempt 1 is FALSIFIED at compile time: Catch2 rejected the
  row-profile `CHECK` logical OR because the whole predicate lacked its
  required extra parentheses. No test ran and no registered band changed.

## Exact resume point

1. Apply only the Catch2 extra-parentheses compile correction for attempt 2.
2. Run focused canonical/nollfio tests, source audit, six benchmark builds,
   CPU subjects, and real CUDA subjects. A missed band is FALSIFIED and leaves
   no further implementation attempt.
3. On green, commit the implementation immediately; then run all 13 mutations,
   supply-chain checks, full 119/119, 119/119, 121/121 matrix, and separate
   docs closure.

The claim most likely to be wrong is completeness of the consolidation
boundary. Repeat the source inventory immediately before the implementation
commit and again before closing F15.
