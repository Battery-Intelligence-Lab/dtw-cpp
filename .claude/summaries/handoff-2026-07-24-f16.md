# Handoff - 2026-07-24 - F16 portable CMake presets

## Accomplishments

- Started from clean F15 closure `07f186a`.
- Re-read F16, the killed-ideas record, archive, LESSONS, TODO reconciliation,
  prior preset/macOS handoffs, and every tracked preset consumer.
- Confirmed the preset declares 3.21.0 while root and pyproject require 3.26,
  and the Windows Clang preset hardcodes
  `C:/Program Files/LLVM/bin/clang++.exe`.
- Ran clean inherited Windows and WSL root configures with all optional/build
  surfaces off. Windows ignored an alternate PATH compiler location and used
  the encoded path; WSL selected `/usr/bin/g++` and `/usr/bin/cc`.
- Confirmed unconditioned presets expose all host-incompatible configure,
  build, and test choices on both Windows and Linux.
- Preserved the binding macOS `/usr/bin/clang{,++}` decision: removing it
  reopens the recorded Homebrew LLVM/HiGHS ABI failure.
- Registered exact metadata, list, alternate-PATH, wrong-host, negative-PATH,
  WSL, mutation, supply-chain, and full-suite bands in
  `.claude/baselines/2026-07-24-f16-cmake-presets.md`.
- Attempt 2 passes the permanent direct and CTest subjects with 81 assertions
  in 4 cases. Windows and WSL expose only their host presets; alternate-PATH
  Release/Debug Windows configures and the WSL GCC configure pass; missing
  `clang++` and both wrong-host presets fail loudly.
- Committed the user-facing repair as `7aef30d`
  (`fix: make CMake presets portable`).
- Mutations M01-M09 fail the real native subject. M10 is FALSIFIED:
  `cmake --list-presets=all` rejects trailing content, but configure-time
  `string(JSON)` accepts the same valid-leading-value document and exits 0.
- Supply-chain inventory remains 39/7/1/27 with 63 focused tests. Canonical,
  llfio-OFF, and Arrow-ON gates pass 119/119, 119/119, and 121/121 with
  6/9/8 capability skips.

## Decisions

- Preserve every public preset name, shared binary directory, generator,
  build/test reference, and the macOS compiler pin.
- Use the complete PATH token `clang++`, not a relocated absolute path or an
  environment-specific macro.
- Add host conditions because a bare Windows compiler token without one would
  silently turn `clang-win` into a Linux Clang preset.
- Keep the permanent gate in the existing native build-metadata subject and
  use CMake's own JSON parser during configuration. Do not add Python to the
  native gate, a new CTest target, or a 28th CMake manifest.
- Update all three current user-facing stale floor statements; do not rewrite
  historical CHANGELOG provenance.
- Treat the unchanged VS17 configure as `[BLOCKED-ENV]` on this VS18-only host.
  No local macOS runtime claim is made.
- Cap implementation at two attempts. All 10 registered mutations must fail.
- Implementation attempt 1 is FALSIFIED before compilation. The configure
  guard observed dependency-mutated `CMAKE_MINIMUM_REQUIRED_VERSION=3.14`
  rather than root's first-command 3.26 and stopped with
  `F16 CMake floor drift: preset=3.26.0, root=3.14, expected=3.26.0`.
  No test ran and no registered band changed.
- Retain the verified portable-preset repair, but do not call F16 closed:
  registered M10 proves the configure-time JSON syntax arbiter is not
  fail-closed. The attempt cap forbids rescue-tuning.
- Route complete-input preset parsing plus the independently reviewed
  top-level `toolchainFile`/UNC-path gap uniquely to F38. F16's unchecked box
  preserves the falsified acceptance state and owns no further implementation;
  resume campaign work at F17.

## Exact resume point

1. Commit the F16 evidence, binding decision, F38 route, TODO reconciliation,
   and this handoff in the separate documentation commit.
2. Verify a clean tree, then begin F17 from `PLAN.md`. Read the existing F17
   evidence in `.claude/baselines/2026-07-23-r1-docs-truth.md`, inventory the
   real CLI checkpoint producer/consumer flow, and register continuation
   semantics plus a real-binary discriminator before implementation.
3. Do not reopen F16 with a third lexical patch. F38 owns a replacement
   fail-closed preset-metadata architecture and its exact trailing-content and
   top-level `toolchainFile` mutations.

The claim most likely to be wrong is portability beyond Windows and WSL.
Source conditions preserve macOS semantics, but only a real macOS rerun would
confirm it. F16 remains formally open because M10 is falsified; `7aef30d`
should be rolled back only by a local revert if its retained product behavior
is later disproved.
