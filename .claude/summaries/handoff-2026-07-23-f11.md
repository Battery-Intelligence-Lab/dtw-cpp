# Handoff — 2026-07-23 — R3-F11 tracked CMake archive integrity

## Current state

- F8 is closed in `1df77fc` / `1883c22`.
- F11 preflight found seven tracked `CPMAddPackage(URL ...)` archives: six
  same-block SHA-pinned dependencies and the one mutable/unhashed example.
- The replacement is preregistered as the compatible 2.0.0rc1 commit
  `eda1b92bc89ee51568b052a6af86f615d336de3c`, archive size 4,928,286, SHA-256
  `d9e991dc05804f5eedebdf3981eb400da229e485f4693ac3225ca17ae4a10696`.
- Two independent upstream download routes reproduced those bytes. The current
  example CMake/main Git blobs equal the rc1 blobs.
- The registered inherited run is red: the production checker exited zero
  while the independent inventory found 25 tracked CMake manifests, seven CPM
  URL archives, and exactly one mutable/unhashed declaration.
- The wider preflight opened F34 for unclassified workflow/container and
  weaker CMake repository acquisitions, and F35 for the unquoted
  `cmake>=3.26` shell redirection. They remain separate from F11.
- The production checker now tokenizes active CMake arguments, scans direct
  and `cmake_language` CPM calls, rejects unclassifiable dynamic/multi-URL
  forms, preserves the 39-action and exact Arrow checks, and enforces the
  registered 25-manifest/seven-archive floors.
- The permanent Python mutation suite passes 42/42 after independent review
  found and forced repairs for five successive false-green classes. The final
  review is CLEAN within the registered CPM URL/hash scope.
- The example now uses the registered rc1 commit archive and SHA-256. A fresh
  nested configure downloaded exactly 4,928,286 bytes with the registered
  digest, exposed `VERSION=2.0.0rc1` and 20 active version definitions, and
  exited zero. Both `dtwc++` and `MY_AWESOME_PROJECT` compile/link.
- The first 60-second configure wrapper is retained as FALSIFIED (exit 124
  after CMake printed generation complete); its build tree is preserved at
  `build/f11-example-pin-timeout-attempt1`. The unchanged second/final attempt
  passed under a longer wrapper.
- Canonical rebuild is clean, the C++ supply-chain subject ran 20 assertions
  in three cases, and CTest is 114/114 with exactly the six registered
  capability skips.
- Generated documentation, the live CLI contract, record hygiene, repository
  hygiene, the production supply-chain gate, Python compilation, and
  `git diff --check` all pass.
- The final pre-commit audit then FALSIFIED the pending checker on dynamic
  argument injection beside a literal URL, `DEFER ID|ID_VAR call CALL`
  parsing, and differently named exact-pin decoys. No commit was made. Five
  new executed mutation cases register the repair floor at 47/47.
- Repair attempt 1 fixed those reproductions but was FALSIFIED at 45/47 because
  two old tests expected weaker diagnostics. The second/final attempt passes
  47/47 and retains the exact 25-manifest, seven-archive, and 39-action
  inventories.
- The HiGHS URL call is now literal. Real CPU and GPU build configurations
  report/cache `CUPDLP_GPU=OFF` and `ON`, respectively; both built, and the
  GPU/HiGHS `test_pdlp_lp` ran and passed. The final canonical suite is 114/114
  with exactly the six registered capability skips.
- The fresh audit confirmed the first three repairs but found two new
  false-greens: semicolon-expanded `cmake_language` arguments and transfer of
  the expected package name to a download-only decoy. The name-only heuristic
  is FALSIFIED. Five new executions register a 52/52 pivot to an exact
  seven-declaration identity inventory.
- Pivot attempt 1 passed 52/52 and closed those bypasses, but two independent
  audits FALSIFIED it because `DOWNLOAD_COMMAND` overrides and ignores a
  decorative registered URL/hash. One audit also found mixed optional/string
  identity sorting loses the verdict to `TypeError`. The second/final pivot
  band is 63/63: ten alternate-source directives plus deterministic
  mixed-name diagnostics.
- The second/final pivot attempt passes 63/63 and the exact production
  inventory (39 actions, seven archives, 25 manifests). Two bounded
  independent audits are reprobing the prior falsifications.
- Both final audits rejected closure. Local probes reproduce two remaining
  false-greens with all live counters green: `CUSTOM_CACHE_KEY` under
  `CPM_SOURCE_CACHE`, and CMake's quoted backslash-newline normalization of
  `"DOWNLOAD_\<newline>COMMAND"`. The no-override fresh example run remains
  valid, but the generic fail-closed claim is FALSIFIED. No third pivot attempt
  is permitted.

## Exact resume point

Rerun final documentation/record/repository/diff gates and commit the verified
partial result as `fix: pin example dependency archive`; do not claim or mark
F11 closed. In a separate `docs:` commit, record the binding falsification,
open the replacement-parser finding, and route the capped F11 work there.
Then resume at R3 F12 rather than rescue-tuning this parser a third time.

Implementation rollback is the single F11 code commit once created. The claim
most likely to be wrong is that the hand-written CMake tokenizer covers every
future metaprogramming spelling; the registered inventory floor and fail-closed
dynamic syntax reduce that risk, while F34 remains the wider acquisition audit.
