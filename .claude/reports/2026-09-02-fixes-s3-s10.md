# Fixes for adversarial findings S3–S10

Source: `.claude/reports/2026-09-02-adversarial-parity-yaml-mex.md`. Touched:
`CMakeLists.txt`, `tests/CMakeLists.txt`, `dtwc/api.hpp`,
`tests/unit/test_tier1_cpp_api.cpp`, new
`tests/integration/test_cli_rejects_unknown_option.cmake`,
`.claude/skills/{check-code-quality,update-docs-skill}.md`.

**S4** — `find_program(matlab)` → `find_package(Matlab COMPONENTS MX_LIBRARY
MAIN_PROGRAM)`; test registered only under `DTWC_BUILD_MATLAB AND Matlab_FOUND
AND Matlab_MAIN_PROGRAM`, `else()` → `message(WARNING "DTWC_BUILD_MATLAB=ON but
MATLAB not found: matlab_suite not registered")`. One-line comment states why
`$<TARGET_FILE_DIR:dtwc_mex>` is legal (same MX_LIBRARY condition creates the
target; genex resolves at generate time). **[confirmed]** `cmake
build/mex-highs-fix` → `Found Matlab: .../R2025b ... components: MX_LIBRARY
MAIN_PROGRAM`, generate OK; `ctest -C Release -N -R matlab_suite` → `Test #131:
matlab_suite / Total Tests: 1`, command uses `.../R2025b/bin/matlab.exe`. Suite
NOT run. WARNING branch itself **[inferred]** (MATLAB is installed here).

**S3** — comment fixed: both parallelisation names are allow-listed, so *at most
these two* may be Incomplete; the list pins neither which nor how many. Floor is
now `DTWC_MATLAB_SUITE_MIN_PASSED` (CACHE STRING, default 60) with a note that it
is loose (observed full run = 100) and must be re-pinned to the measured count
alongside the AGENTS.md MATLAB inventory. **[confirmed]** `>= 60` reaches the
generated command.

**S5** — comment at `CMakeLists.txt:~62` extended: directory scope means every
target in a `DTWC_BUILD_MATLAB=ON` tree gets the define (dtwc++, mip-solvers,
fetched HiGHS/llfio, dtwc_cl, tests, Python module), deliberately, since mixing
defining/non-defining TUs is IFNDR; `std::mutex` layout unchanged (same
`_Mtx_storage`), only the ctor's constexpr-ness. No behaviour change.

**S6** — added `load(const path&, int, char, string_view = "") = delete;` and the
`series_type` twin. **[confirmed]** full `cmake --build build/nollfio` → exit 0;
**no existing caller hit a deleted overload**, so nothing was mis-bound.
The brief's non-dependent `static_assert(!requires(fs::path p){ dtwc::load(p, 0,
','); });` **fails on clang 19**: `error: call to deleted function 'load'` (hard
error) *and* the assert fails. Working form is a *dependent* requires-expression:
`template <class Source> concept dtwc_load_binds_char_as_skip_rows = requires(Source s){ dtwc::load(s,0,','); } || requires(Source s){ dtwc::load(s,1,',',"name"); };`
asserted false for `fs::path` and `Dataset::series_type`, plus positive controls
on the 5-arg shape. Discriminating: on a standalone probe with the deleted decls
removed clang reports `static assertion failed due to requirement
'!LoadsCharThird<std::filesystem::path>'`. Also compiles under MSVC 14.50.

**S10** — new `.cmake` script (placed in `tests/integration/` with the other
`.cmake` gates, not `tests/` root — deviation from the brief's literal path, for
consistency). Runs the real binary with the option, requires a non-zero *numeric*
exit, requires the flag to be named in stdout+stderr, rejects skip text, prints a
marker; CLI11 phrasing no longer pinned. **[confirmed]**
`CLI_REJECTS_UNKNOWN_OPTION subject=real_dtwc_cl option=--yaml-config exit=109
nonzero=1 named=1 skips=0`; negative control `-DOPTION=--help` (accepted, exit 0)
→ FATAL_ERROR.

**S9** — dead `rg` for the deleted `TODO: This always overrides CLI values...`
string replaced by `rg -in 'yaml' dtwc/dtwc_cl.cpp` (zero hits now; any hit means
the loader is back). `update-docs-skill.md:25` → "TOML config options".

## Gates
- `cmake --build build/nollfio` → exit 0.
- `ctest -R "tier1|cli_rejects|unit_test_cli_args"` (nollfio) → **3/3 passed**.
- `cmake build/mex-highs-fix` → OK; `-N -R matlab_suite` → 1 listed (not run).
- `uv run python scripts/check_docs_contract.py` → exit 0, `documentation
  contract checks passed`. S1's `--checkpoint-interval` red is gone.

## Proposed CHANGELOG bullet (not edited — not mine)
- `dtwc::load()` now rejects the pre-2.0 `load(source, skip_cols, delimiter)`
  argument shape at compile time instead of silently binding the delimiter char
  to `skip_rows`.

## Unresolved
- Floor left at 60 (observed 100); raising it belongs to the MATLAB parity owner.
- MATLAB-not-found WARNING path unexercised **[inferred]**.
- Stale `DTWC_MATLAB_EXECUTABLE` cache entries remain; unused, harmless.
