# F17 real-CLI binary-result resume contract - 2026-07-24

## Scope and base

- Base: `db129e5032614d2272ebd10e426126fb0cfb3a4d`
  (`docs: record F16 portable preset verdict`).
- `git status --short` produced no output.
- Subject: the real `dtwc_cl --resume` consumer of the automatic
  `<output>/<name>_checkpoint.bin` binary `ClusteringResult`.
- Out of scope: changing binary format v1, iterative algorithm-state
  serialization, streamed FastCLARA continuation, dense directory checkpoint
  v2, mmap cache v3, and the R5 checkpoint/config fuzz campaign.

No F17-specific killed idea exists. The archived M49 decision leaves binary
checkpoint fuzzing to R5, while the frozen 2.0 contract preserves the binary
artifact and requires F17 to make the existing CLI consumer real.

## Confirmed inherited state

`dtwc/dtwc_cl.cpp:1398-1405` loads all five result fields into block-local
`ckpt_result` and reads only cost/iterations for a verbose message. Every
clustering branch at lines 1551-1682 then executes, and lines 1684-1688
unconditionally replace the same checkpoint with the fresh result. The loaded
labels, medoids, and convergence flag have no consumer.

The production loader at `dtwc/checkpoint.cpp:682-750` exposes only a complete
`ClusteringResult`: labels, medoids, total cost, iteration count, and
convergence. Binary v1 contains no algorithm name, data/configuration identity,
RNG state, restart/sample cursor, PAM swap cursor, CLARA sample state,
dendrogram, or solver state. The automatic file is written only after a
clustering method returns.

The inherited serializer unit test passes but proves no CLI reachability:

```text
All tests passed (9 assertions in 1 test case)
```

The inherited CLI helper gate remains:

```text
Filters: [cli]
Randomness seeded to: 1
===============================================================================
All tests passed (149 assertions in 25 test cases)
```

An exploratory real PAM control on the tracked 27-series conformance fixture,
with `--max-iter 1 --n-init 1 --seed 42`, produced these artifacts under the
ignored `build/f17-audit-fresh-one/` directory:

```text
cost=976 iterations=1 converged=0 medoids=20,4,15
```

Its labels are cluster 1 for rows 1-9, cluster 2 for rows 10-18, and cluster 0
for rows 19-27. This run characterises the fresh discriminator; it is not
acceptance evidence until the registered real-CLI gate reproduces it.

The tracked input identities before implementation are:

```text
tests/conformance/data/conformance_series.csv SHA256=6230423623F0564D3CA0F8C27D78D5D7CEB863DCC3332BBAE9FFE2B78B869F3F
tests/conformance/conformance.toml SHA256=7F36DB93AB26BCB7DC8D393671221BA1FD2190B567A790E011B3FC0B16DDCD55
```

## Binding semantics registered before implementation

`--resume` means **completed-result replay**, not iterative continuation:

1. Load `<output>/<name>_checkpoint.bin`, validate it against the current input
   count and requested cluster count, and restore all five result fields.
2. Skip every clustering method. A `converged=false` file is a completed
   iteration-capped result and is replayed as such; `--max-iter` is not an
   additional budget.
3. Rehydrate `Problem` labels, medoids, and cluster count so the ordinary output
   and any already-available scoring route consume the restored result.
4. Do not rewrite the binary source checkpoint during replay.
5. A missing, malformed, or structurally incompatible requested result is a
   nonzero actionable error, never a fresh clustering fallback.
6. Dense directory checkpoints and mmap caches remain independent distance
   persistence mechanisms. Neither requires `--resume`.

PAM-only warm continuation is rejected for F17. It would invent a method-specific
meaning for a generic result file written by every successful method, and the
file lacks enough state to implement the same promise for OneBatchPAM, CLARA,
Lloyd, MIP, LR-core, TADPole, or hierarchical clustering. Streamed assignment
continuation remains explicitly owned by R5/2.1.

Binary v1 cannot authenticate input order, values, configuration, or producing
method. F17 therefore validates shape and fields, then trusts the explicitly
selected `<output>/<name>` result under a documented same-input-order and
same-configuration precondition. Format migration or authenticated provenance
is not silently added to this frozen-format repair.

## Registered deterministic fixture

A `.cc` fixture writer must call the production
`save_binary_checkpoint`/`load_binary_checkpoint` functions. Its valid
27-series, k=3 result is deliberately coherent but distinguishable:

| Field | Registered value |
|---|---|
| medoids | `[0, 9, 18]` |
| labels | rows 1-9 -> 0, 10-18 -> 1, 19-27 -> 2 |
| total cost | `1650` |
| iterations | `41` |
| converged | `false` |
| serialized size | `152` bytes |

The cost is independently checkable on the non-degenerate tracked fixture:
each of its three translated groups has distances
`[0,16,24,42,56,76,96,112,128]` to its first member, whose sum is 550;
three groups give 1650.

## Decisive bands

The permanent CMake script must drive the built `dtwc_cl` binary, never a helper
copy. It runs one fresh control, verbose and quiet valid replays, and nine
negative replays: missing file, malformed header, wrong N, wrong k, out-of-range
label, out-of-range medoid, duplicate medoid, negative iterations, and
non-finite cost.

Before the first decisive run, review strengthened the two positive routes
without changing the registered run/count bands:

- the verbose replay requests PAM and must emit exactly the preserved binary,
  labels, and medoids—no distance matrix, silhouette, or mmap artifacts;
- the quiet replay requests `kmedoids`, imports the fresh control's tracked
  distance-matrix artifact, and must still replay the same five fields. Its
  silhouette values must equal the fresh control after ignoring cluster-ID
  renumbering. This proves the contract is method-generic and that the loaded
  labels, medoids, and k were rehydrated into `Problem`, rather than merely fed
  to the direct CSV writers;
- the fixture writer moves each source checkpoint timestamp back 24 hours
  before invocation. An identical accidental rewrite therefore changes the
  timestamp even on a whole-second timestamp interface; SHA-256 remains the
  independent byte-preservation check.

The sole green marker is exactly:

```text
F17_CLI_RESUME subject=real_dtwc_cl writer=production_serializer runs=12/12 replay_fields=10/10 markers=2/2 algorithm_skipped=1/1 fresh_discriminator=4/4 checkpoint_preserved=10/10 rejection_cases=9/9 sources_preserved=2/2 skips=0
```

Required observations behind that marker:

- both replay outputs reproduce labels, medoids, cost 1650, iterations 41, and
  `Converged: no`;
- the verbose replay contains no `Running FastPAM`, while both verbose and quiet
  replays contain the unconditional completed-result marker;
- the quiet replay names `kmedoids` as its requested method, yet reports
  `checkpoint replay` as the result source and emits the expected silhouette
  values from the imported matrix;
- the fresh control reproduces cost 976, iterations 1, medoids `[20,4,15]`,
  and its labels, medoids, cost, and iterations all differ from replay;
- SHA-256 and last-write timestamp of every existing checkpoint are unchanged
  after replay/rejection; the missing case remains absent;
- every negative case exits nonzero, reports `Error:`, produces no result CSV,
  and never reaches clustering;
- the two tracked input hashes remain exactly the registered values.

The pure validator must add at least eight assertions in one new `[cli][resume]`
case, taking the focused CLI helper floor to at least 157 assertions in at least
26 cases. It covers the valid control and all seven structural field/shape
rejections.

Adding one real-CLI CTest changes the full-suite bands to:

- canonical `build/highs-1151`: **120/120**, zero failed, 6 capability skips;
- llfio-OFF `build/nollfio`: **120/120**, zero failed, 9 capability skips;
- Arrow-ON `build/arrow-pyarrow-23`: **122/122**, zero failed, 8 capability
  skips.

Supply-chain inventory remains 39 URL pins / 7 archive pins / 1 clone pin /
27 CMake manifests, with 63 focused tests. Documentation generation/checking,
`bash -n scripts/slurm/jobs/checkpoint_test.slurm`, and the existing binary
round-trip test must pass.

## Registered mutations

Each non-equivalent mutant must make the focused real-CLI or validator subject
fail before restoration:

| ID | Mutation | Required killed observation |
|---|---|---|
| M01 | discard the loaded result and execute the fresh route | replay fields/fresh discriminator fail |
| M02 | execute a clustering branch after selecting replay | exact replay or algorithm-skipped check fails |
| M03 | rewrite the binary checkpoint on replay | last-write timestamp changes |
| M04 | print the replay marker only under `--verbose` | quiet marker check fails |
| M05 | remove input-count validation | wrong-N case exits zero |
| M06 | remove cluster-count validation | wrong-k case exits zero |
| M07 | remove label-domain validation | bad-label case exits zero |
| M08 | remove medoid-domain validation | bad-medoid case exits zero |
| M09 | remove unique-medoid validation | duplicate-medoid case exits zero |
| M10 | remove iteration validation | negative-iteration case exits zero |
| M11 | remove finite-cost validation | non-finite-cost case exits zero |
| M12 | treat a failed load as fresh execution | missing/malformed case exits zero |

Implementation is capped at two attempts. No registered band may be weakened
after a run. A falsified band is recorded and F17 remains open.

## Rollback and weakest claim

Rollback is a local revert of the future F17 implementation commit; binary v1
bytes and all data fixtures remain unchanged. The claim most likely to be wrong
is that explicit `<output>/<name>` selection is an adequate provenance boundary.
A same-N/k checkpoint from different data can pass structural validation because
v1 has no identity; only a separately authorized format migration could prove
that association.
