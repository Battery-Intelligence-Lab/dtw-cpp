# F10 signed/degenerate D-sampling — 2026-07-23

## Registered band

The band was written before execution in
`.claude/baselines/2026-07-23-r0-adjudication.md`:

- selected and unselected non-finite distances must throw;
- nonnegative input and selected-zero behavior are exact;
- signed translation and degenerate zero totals are exact on binary64 fixtures;
- seeded/unseeded k-means++ and seeded FastPAM signed/degenerate branches run;
- the complete current canonical gate passes (114 targets after adding this
  suite), with exactly the six documented capability skips.

## Red before repair

Command:

```powershell
build/highs-1151/bin/unit_test_distance_sampling_weights.exe
```

Decisive output, verbatim:

```text
distance_sampling_weights rejects a non-finite selected distance
  NaN
...............................................................................

C:/D/git/dtw-cpp/tests/unit/core/unit_test_distance_sampling_weights.cpp(241): FAILED:
  CHECK_THROWS_AS( weigh(distances, selected), std::runtime_error )
because no exception was thrown where one was expected:

C:/D/git/dtw-cpp/tests/unit/core/unit_test_distance_sampling_weights.cpp(243): FAILED:
  CHECK_THROWS_WITH( weigh(distances, selected), ContainsSubstring("initialization distance must be finite") )
because no exception was thrown where one was expected:

distance_sampling_weights rejects a non-finite selected distance
  positive infinity
...............................................................................

C:/D/git/dtw-cpp/tests/unit/core/unit_test_distance_sampling_weights.cpp(249): FAILED:
  CHECK_THROWS_AS( weigh(distances, selected), std::runtime_error )
because no exception was thrown where one was expected:

distance_sampling_weights rejects a non-finite selected distance
  negative infinity
...............................................................................

C:/D/git/dtw-cpp/tests/unit/core/unit_test_distance_sampling_weights.cpp(255): FAILED:
  CHECK_THROWS_AS( weigh(distances, selected), std::runtime_error )
because no exception was thrown where one was expected:

===============================================================================
test cases:  8 |  7 passed | 1 failed
assertions: 51 | 47 passed | 4 failed
```

**Verdict: RED reproduced.** The implementation validated finiteness only after
skipping selected slots.

## Repair

`core::distance_sampling_weights` now validates each converted input before
selection masking. Selected entries remain exactly zero in the output and do
not affect the shift or total, but poison can no longer hide in them.

The test explanation was also corrected: a common additive shift preserves
ordering and pairwise differences, not proportional sampling probabilities.
R2-D13 still owns the research decision on the signed-weight distribution.

## Focused green evidence

Direct seam suite, verbatim:

```text
===============================================================================
All tests passed (51 assertions in 8 test cases)
```

Seeded FastPAM filter, verbatim:

```text
Filters: [seeded]
===============================================================================
All tests passed (12 assertions in 3 test cases)
```

Degenerate and signed k-means++ filters, verbatim:

```text
Filters: [degenerate]
===============================================================================
All tests passed (8 assertions in 2 test cases)

Filters: [softdtw]
===============================================================================
All tests passed (7 assertions in 1 test case)
```

Full changed suites from their CTest working directory (`C:/D/git/dtw-cpp`),
verbatim:

```text
All tests passed (51 assertions in 8 test cases)
All tests passed (76 assertions in 14 test cases)
All tests passed (82 assertions in 14 test cases)
```

An exploratory direct `unit_test_fast_pam` invocation from
`build/highs-1151` failed one case because the test writes to
`.\results/fast_pam_testmedoids_rep_0.csv`; rerunning from its registered CTest
working directory passed 76/76. This is a test-working-directory dependency,
not evidence of algorithm failure, and is recorded for the R3 error-path/test
robustness sweep rather than hidden.

## Canonical gate

Build first and last emitted lines, verbatim:

```text
[0/2] Re-checking globbed directories...
[113/114] Linking CXX executable bin\unit_test_wave2b_integration.exe
```

CTest decisive output, verbatim:

```text
100% tests passed, 0 tests failed out of 114

Total Test time (real) =  28.83 sec

The following tests did not run:
	 48 - test_cuda_correctness (Skipped)
	 50 - test_cuda_lb_keogh (Skipped)
	 54 - test_io_readers (Skipped)
	 55 - test_metal_correctness (Skipped)
	 56 - test_metal_lb_keogh (Skipped)
	 57 - test_metal_mmap (Skipped)
```

**Verdict: PASS.** 114/114 registered targets passed; zero failed; exactly the
six preregistered capability skips remained.

## Most-likely-wrong claim

The signed translation is safe to *feed to the sampler*, but its statistical
choice is not yet blessed: adding a constant changes proportional weights.
R2-D7/D13 must either justify that distribution or replace it.
