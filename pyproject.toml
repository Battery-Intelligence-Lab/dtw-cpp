[project]
name = "dtw-cpp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cmake>=3.21",
    "ninja>=1.13.0",
    "numpy>=2.4.2",
    "prebuilt-binaries>=0.5.0",
    "pybind11>=3.0.1",
    "setuptools>=80.10.2",
    "wheel>=0.46.3",
]
